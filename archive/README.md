# archive/ — frozen historical docs (NOT current)

Do not read these as current state. They are kept for history only.

- **HANDOFF.b24-snapshot.md** — the original deep briefing, frozen at build **b24** (file map still
  says "built b21"). It predates the newer systems: it lists only **2 characters** (Pepe/Trumpe),
  the **old netcode** (host 40Hz / guest 33Hz, INTERP_DELAY 68ms, **no WebRTC P2P**), and Sydney-only.
  Its still-current infrastructure facts (Supabase URL, `sql/` run-order, Fly app name, the
  service_role rotation warning, gotchas) were migrated into **PROJECT_BRIEF.md** before archiving.
  Left byte-for-byte intact because the original was marked read-only.

Current docs live one level up: **CLAUDE.md** (laws), **STATE.md** (living state), **PROJECT_BRIEF.md** (deep reference).
