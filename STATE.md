# STATE.md — Based Cup living state log

Read **CLAUDE.md** first (laws/build/deploy). Deep internals are in **PROJECT_BRIEF.md**. This file = current state only.

> **▶ NEXT (handoff @ b107):** b107 = THREE contained cleanups. **(1) SOCIAL + STORE → corner ICON buttons.** Moved out of the lobby button rails into small 42px circle icons (styled like the bell): SOCIAL 👥 top-LEFT (tucked under the account chip, `#socialBtn.cornerIcon{top:60px;left:12px}`), STORE 🛍️ top-RIGHT in the row with the bell+X (`#storeBtn.cornerIcon{top:12px;right:112px}` → BELL right:12 · X right:62 · STORE right:112, even 50px spacing, no overlap). Same IDs + handlers kept (openSocial / store overlay). They're `#stage` children at z55, shown on menus + hidden in-match (mirrors `#xBtn` via new `setCornerIcons()` at the 3 xBtn toggle sites); other overlays (z56) cover them, the lobby (`#startScreen` z20) doesn't → visible only where intended. Portrait acctBtn reserve widened `calc(100vw-122px)`→`-172px` for the extra right icon. **(2) LOCAL 2-PLAYER restored (desktop only).** New `#local2pBtn` "🎮 LOCAL 2P" in railL launches the existing `mode==='2p'` machinery (`beginGame()`, respects the lobby MODE selector); hidden on touch (`IS_TOUCH`). CONTROLS — **P1: WASD move · SPACE kick · F lunge · ShiftLeft sprint**; **P2: Arrows move · ENTER kick · / (Slash) lunge · ShiftRight sprint**; both aim by FACING (keyboard-only, no mouse — `setAim` P1 now uses movement heading in 2p). Fixed a latent shift cross-trigger (generic `keys['shift']` is set by EITHER shift, so P2's ShiftRight also sprinted P1 → P1 now uses ShiftLeft-only in 2p; single-player keeps either-shift). Space/Enter/Slash get `preventDefault` during play (no page-scroll / focused-button re-activation). Single-player / online / CPU-practice controls unchanged. **(3) ROSTER CHECK — pepe/trumpe: PRESENT & selectable, no fix needed.** They inject via `__PEPE__`/`__TRUMPE__` (from `pepe_cut_s.png` 71KB / `trumpe_cut_s.png` 104KB, both present, not dehydrated) — SEPARATE from the `EXTRA_CHARS` list, so build.py's "Extra characters live: [12]" is just the extras printout, NOT the full roster. `ROSTER` (14) and `STATS` both include pepe+trumpe; the locker carousel renders all 14. So 14 total = pepe + trumpe + 12 extras. ⚠️ Preview still wedged for this animated-canvas app (window reported 0×0) — verified via computed geometry (no overlaps, even icon spacing), 0 console errors, harness, and a control-scheme sim (`sim_2p.cjs`, 16/16 pass). ---- (earlier) b106 = RED-CARD (and ALL) match-end made RELIABLE online — a red card (2nd yellow→red→removal→team at 0→endGame) now ends the match and shows the standard REMATCH screen for BOTH players. Root cause was online-only (and it also affected time-up ends): the host announced the end ONLY via a loss-tolerant, UNORDERED DC snapshot (`createDataChannel('game',{ordered:false,maxRetransmits:0})`) carrying `st:'win'`, and the SAME transition frame ALSO fired the steady-60Hz `sendState()` at L2962 with the default `st:'play'` — the guest reads only the NEWEST buffered snapshot (applyNetState L4653), so the trailing play OVERWROTE the win → guest hung on the play screen (no rematch; host already on the win screen = desync); on a lossy link the lone win packet could also just be DROPPED. FIX (3 parts): (1) gate the L2962 trailing send on `state==='play'` so the win stays the newest DC frame (halftime still streams — `state` stays `'play'`); (2) `endGame` ALSO sends a RELIABLE WS control msg `NET.relay({end:who})`; (3) guest `onRelayData` handles `d.end` idempotently (both paths guard `state==='play'`). Offline red→rematch was ALREADY fine (shared `winScreen`/`setupEndScreen`; `newGame()` clears `out`/`cards`; the red flash is `pointer-events:none`). Netcode-only; no gameplay/stat change; IDs preserved (harness passes). ---- (earlier) b105 = MAIN-MENU card redesign + six ore tiers + sprite shrink (lobby showcase only; visual). Six emoji ore bands (🪵Wood75-79 🪨Stone80-83 ⚙️Iron84-87 👑Gold88-90 💎Diamond91-93 💀Netherite94-100) via `oreTier`/`tierEmoji`; the `#lobbyCard` is now a narrow FIFA-white PORTRAIT card = NAME + OVR + ore EMOJI + OFFENSE ★/5 + DEFENSE ★/5 (real half-stars; `offStars`=(pace+drb+pow+div)/16, `defStars`=(def+sta)/8), back = the fuller record (flip works). Lobby live sprite shrunk ~half in `lobbyAnimDraw` (`R=min(H2*.185,W2*.095)`) so he roams. Locker keeps its detailed layout — only the shared ore bands updated (added `.loSlot.tier-wood`, re-spaced). Tune the card size via `#lobbyShowcase` width / `.lobbyCard` aspect-ratio; sprite size via the `R=` line. ---- (earlier) b104 = GUEST MOBILE MOVEMENT "ankle weight" fix. Root cause = the guest's own-player reconciliation in `predictGuestSelf` used `err>60?0.16:0.05`, but the guest's correct client-prediction lead over the freshest authoritative snapshot is ~`v*RTT`≈90px at the ~300ms relay floor — so the firm 0.16 tier fired during NORMAL movement and dragged the local prediction back toward the RTT-delayed position (worst on stops/reversals: a ~6px/frame wrong-way jerk). The lunge escaped because its burst dwarfs the pull. FIX (one line): `k = err>150 ? 0.16 : 0.04` so the local prediction leads in real time; firm tier only catches genuine drift (>150), hard-snap >200 kept. Guest-only — host movement, opponent interp (b84), and the lunge are untouched. Tune `predictGuestSelf`'s reconcile k/thresholds if a guest ever feels too floaty (raise k) or still weighted (lower the gentle k / raise the firm threshold). ---- (earlier) b103 = MOBILE CHROME CLEANUP (4 visual fixes). (1) Profile chip covered menu back buttons → `.overlay` raised to z56 (above the z55 chip), `#startScreen` pinned z20 (chip still shows on the lobby, covered on every other overlay); acctBtn width-capped + name-ellipsis in landscape. (2) BACK/CLOSE buttons (`button.btn.ghost`) bigger + clearer (min-height 48, `#4fae6e` border), locker round-back 50px. (3) In-match ping pill moved from mid-screen (`156px`) to bottom-centre (`enterMatchChrome`; pot stays lifted). (4) Landscape lobby breathing room (bigger gaps/rails/showcase/title) while keeping the PLAY bar one-line at 667 so it fits with no scroll. ⚠️ Screenshot tooling still wedged — verified via `getBoundingClientRect`/`elementFromPoint` at 740×360 / 667×375 / 820×360 + desktop. ---- (earlier) b102 = MOBILE-LANDSCAPE lobby REFIT + guest SOL prompt ✕/persist. Landscape phones (width >640) were falling through to the DESKTOP layout → oversized/scrolling/PLAY off-screen; added `@media (orientation:landscape) and (max-height:500px)` that lays the lobby out horizontally and sizes the showcase by `vh` so it fits the short viewport (no scroll, PLAY always reachable). Guest prompt got an ✕ (`#guestBannerClose`) + `guestPromptDismissed` session flag (no re-nag) and a compact landscape pill (message hidden via `.gbMsg`) docked above the PLAY bar clear of all controls. Desktop + the b96 portrait lock untouched. ⚠️ Screenshot tooling still wedged (animated lobby canvas) — verified via `getBoundingClientRect` at 740×360 / 667×375 / 820×360 + desktop. b101 (still current below): keep-away/mode-select restored via lobby TYPE+MODE selectors + one PLAY. ---- (earlier) b99 = two fixes. **FIX 1 (right-click/pause routing):** root cause = the `contextmenu` preventDefault + lunge triggers were `<canvas>`-scoped, so right-clicks landing off the canvas (HUD/cards/pause btn/letterbox margins) popped the native browser menu and didn't lunge (inconsistent = depended on the pixel hit). Fixed with ONE document-level `contextmenu` handler that suppresses the menu across the whole play area (`state==='play'||'paused'`) and fires the lunge during play — right-click is now lunge-ONLY; menu/pause stays on Esc (already the only Esc binding; tooltip updated). Mechanic + b97 cards untouched. **FIX 2 (wager routing — DIAGNOSED, NOT changed):** wager is NOT mis-coded — it runs the full peer handshake and stakes coins (verified two wager players → stake 5). It shares the relay's single FIFO `quickQueue` with quick (both send `{t:'quick'}`), so it opportunistically matches quick players → `min(N,0)=0` casual — that's why it "appears to queue into quick." A dedicated wager queue is a `lobby/server.js` change + manual `fly deploy` (and would starve the small wager pool); left as-is per the task's "report if it just shares UI" guidance. ⚠️ Screenshots + real-time rAF still wedged in this backgrounded preview (verified via in-page event dispatch + handshake logic sims). ---- (earlier) b98 = LUNGE INPUT ROBUSTNESS (right-click dive reported dead on desktop). The b85 input code was byte-identical + fires in every reproducible test (verified the full chain incl. driving the real `loop()`), and nothing covers the canvas — no code regression was reproducible in the wedged headless preview. Root cause = relying solely on a right-button `mousedown` (`e.button===2`), which some desktop browsers/devices don't deliver, leaving `lungeReq` unset while keyboard movement still works. FIX (input only): the lunge now ALSO fires on `contextmenu` (canonical right-click, always fires; menu still suppressed) and `auxclick` — 3 idempotent signals. Mobile LUNGE button confirmed working. Mechanic/tuning + b97 cards untouched. ⚠️ Screenshots + real-time rAF are wedged in this backgrounded preview (verified via in-page event dispatch + `loop()` driving). ---- (earlier, still current) b97 = REGRESSION FIX (foul/card): the CPU was racking up 50+ yellows and never being sent off. ROOT CAUSE = the goal-kickoff & half-time `reset()` rebuilt players with `cards:0` and never re-stamped the yellow/red counts (latent since b85, surfaced by b89's livelier CPU lunging — the b90–b96 lobby builds did NOT touch the foul/AI path), so every goal wiped the count and the 2-yellow→red→removal threshold was unreachable. FIX: `reset()` now preserves `cards`+`out` across the rebuild; `newGame()` clears them for a fresh match. Verified (deterministic in-page sims — real-time rAF + screenshots are wedged in this backgrounded preview): cards persist across a goal, 2nd yellow → red → removal → win, count can't exceed 2, one foul = +1, b89 CPU-foul gate active. NO netcode/gameplay/AI-tuning change. ---- (earlier, still current) b96 = LOBBY POLISH (5 tweaks, see the b96 log below): ground-line face crop (`LOBBY_GROUND_CUT=0.50` — head-only for ~12/14; pdidpe/elon show a soft collar hint, tune the cut or per-char/re-cut those two if wanted), endless roam (-0.05..1.05), lower juggle (`LOBBY_JUGGLE_LOW`), Phantom prompt → FIFA-clean bottom-left, and a GLOBAL enforced landscape lock (portrait blocked lobby + in-match). ⚠️ Screenshot tooling was wedged AGAIN this session (hangs even on a static page) — the crop was verified via per-sprite `getImageData` silhouette analysis, so **recommend a quick human eyeball of the 14 lobby heads live**. STILL PENDING separate builds: **(a) apply the head-crop GLOBALLY (in-match); (b) move Social/Store/etc. to Fortnite CORNER ICONS.** ---- (earlier) b95 fixed mobile in-match controls (full-tilt joystick = full normal speed; dedicated ⚡RUN sprint button, no rim-sprint). Tune touch feel via `TOUCH_MOVE_DEADZONE`/`TOUCH_MOVE_FULLTILT`; sprint button pos in `#sprintBtn` CSS. Earlier: b94 = lobby free-roam character + lobby-only face crop (feathered ellipse, `LOBBY_HEAD` per-char config) + slower ease + deeper ball arc. ⚠️ SCREENSHOT tooling was wedged in the b94 session — recommend a quick human eyeball of the 14 lobby heads live + tune `LOBBY_HEAD[<char>]` if any clips hair/shows a shoulder. PENDING separate builds: (a) apply the head-crop GLOBALLY (in-match); (b) move Social/Store/etc. to Fortnite CORNER ICONS. Full-screen lobby: swappable `#lobbyBackdrop`; invite slot is a 2v2 PLACEHOLDER; living-char animation tunables in `lobbyAnimStep` (move durations in `lobbyPickMove`). Future UI phases: add more swappable backdrops; reskin the OTHER screens (mode hub, social, profile, wager, win) to the same FIFA-clean system; wire the invite slot to the real party system WHEN 2v2 ships. b89 fixed the AI lunge decision (CPU no longer fouls itself out — see the b89 log; tune via `LUNGE_AI_FOUL_MARGIN`/`LUNGE_AI_YELLOW_CAUTION`/`D.lunge`/`D.lungeSloppy` if needed). Still pending SQL: ⚠️ **run the `merge_char_stats` SQL once in Supabase** (b88 chat / `supabase-schema.sql`) to enable cross-device per-character stat sync — until then per-char stats stay LOCAL (never wiped). Still pending from b86: run `merge_profile` SQL. b87 shipped five contained UI/logic fixes (see the b87 log). Optional follow-ups: (a) if solo / 2P-local practice modes are still wanted, relocate that toggle (b87 made Practice straight-in vs-CPU); (b) the future global text-style pass (listed in the b87 log). Still carried from b86: ⚠️ **user must run the `merge_profile` SQL once in the Supabase SQL editor** (printed in the b86 chat) to re-enable cloud xp/wins/losses sync — until then the b86 client safely keeps xp local-only and **never wipes it** (the reset bug is already gone). Then confirm a match raises xp on both devices and that no device wipes another to level 1. Also pending: **feel-test the b85 lunge** (right-click desktop / blue LUNGE button mobile) — tune via the named constants if needed (`LUNGE_DIST_BASE/PER_DIV` reach, `LUNGE_DEF_BONUS` defender reach, `LUNGE_COOLDOWN`, `LUNGE_STAMINA`, foul `FOUL_CONTACT_DIST/BALL_REACH/BALL_MARGIN`, CPU `LUNGE_AI_RANGE` + AI `D.lunge`). 2v2 multi-player foul cases become testable only when 2v2 ships (rule is built general). Also still pending from b84: **test b84 guest smoothness** in a real online match — if still choppy, nudge the named tunables (`INTERP_FLOOR`↑ for more buffer, `RENDER_EASE`) — no re-diagnosis needed (see b84 log). **Do NOT re-chase the online ping in code** — the ~300ms relay floor is a *network-side* limit (their two machines are symmetric-NAT/CGNAT; sub-80 needs a direct connection: off-CGNAT / hotspot / UPnP), not a bug (see b82). TURN is wired + live (Vercel env vars `TURN_KEY_ID`+`TURN_KEY_API_TOKEN`); `P2P_DEBUG=false` (flip true to re-diagnose P2P).

> **🔎 DIAGNOSIS (✅ FIXED in b101 — see the b101 log) — "keep-away mode + the Goals/Keep-Away select DISAPPEARED":** **Nothing was deleted — keep-away gameplay is 100% intact and the toggle still exists in the DOM; it's just ORPHANED from every common entry flow.** (1) The ⚽GOALS/⏱️KEEP-AWAY toggle is `#modeSeg` (`game_template.html` ~1238-1241), which lives inside `#selectScreen` (the old character/mode select overlay, ~1220). It's still fully wired: `seg('modeSeg',…)` (~3915) sets `gameMode`, and `selStartBtn` (~3917) launches with it. (2) But `#selectScreen` is now reached from **exactly ONE place**: `openSelect('host')` (~4185) — the online-FRIEND host lobby (only after a friend joins your private code). Every other flow **bypasses it and hard-codes `gameMode='goals'`**: `msQuickBtn` (~3537) + wager `.wsAmt` (~3536) both `gameMode='goals'; startQuickMatch()`, and `practiceBtn` (~3936) `gameMode='goals'; beginGame()`. So Quick, Wager, and Practice can NEVER be keep-away. (3) **When it regressed:** the origin is **b70 (Locker BUILD 1)** — `git log -S` shows b70 introduced the "queue straight in / no picker" bypass for quick+wager (hard-coding goals); **b87** extended the same straight-in to Practice; **b90–b96 (lobby rebuild) did NOT remove any mode code** — it just wrapped these already-bypassing handlers behind the lobby `lobbyQuick/lobbyWager/lobbyFriend` chips (`.click()`→`ms*Btn`), which is where the user *noticed* the loss. So it predates the lobby rebuild. (4) **Keep-away gameplay is entirely present** and gated only on `gameMode==='keepaway'`: `KEEPAWAY_TIME=90`/`matchLen()`, possession scoring (`possP/possT`, `onTimeUp`→`endGame(possP>=possT…)` ~2369), possession-meter HUD (`setMeta` ~1675), slower ball-holder (×0.75), keep-away AI poke-away + rebound bonus (`recordRebound`), win-screen text (~2415), "Ball Hog" achievement (`keepaway1`), `kaWins` stat. **RESTORE PLAN (clean, and YES it combines cleanly with "rebuild the bottom buttons as SELECTORS"):** add a Goals/Keep-Away **selector to the lobby** (reuse the `.seg` style + the existing `seg()` helper that already sets `gameMode`), then **delete the three `gameMode='goals'` hard-codes** at ~3536/3537/3936 so Quick/Wager/Practice respect the lobby selection (`newGame()`/`matchLen()` already read `gameMode` — no launch-path reset to fight). Redesign the bottom as: **[type selector: Quick/Wager/Friend] + [mode selector: Goals/Keep-Away] + one PLAY** — PLAY dispatches by type (quick→`startQuickMatch`, wager→`openWagerSelect`→stake→queue, friend→`openOnlineLobby`) with `gameMode` already set. Online is safe: the host's `gameMode` rides `sendHostStart`→`mode`→guest `d.start.mode` (~4199), so both peers play the host's mode (same pattern as the wager stake). **HARD SAFETY:** keep the existing IDs (`modeSeg`/`selectScreen`/`ms*Btn`/`selStartBtn`) in the DOM so the harness passes — hide the old hub if replaced, don't delete referenced IDs. Friend-host keep-away already works today via `openSelect('host')` — leave that path intact (or unify it with the new lobby selector). No gameplay change needed; it's purely restoring the selection step.

> **🔎 DIAGNOSIS (✅ FIXED in b86 — see the b86 log) — "player level randomly resets to 1":** ROOT CAUSE is **NOT duplicate accounts** — it's a **destructive two-way xp sync with `||0` and no max()-merge**. (a) `ensureOnline()` (every lobby entry) does `profiles.upsert({xp:account.xp||0,...})` = LOCAL→CLOUD overwrite; an empty-localStorage context (new device, cleared cache, private window, or a *different origin* — prod domain vs *.vercel.app preview, since localStorage is per-origin) pushes xp:0 over real cloud progress. (b) `signInOnline()` does the reverse: local `xp:(prof.xp)||0` = CLOUD→LOCAL overwrite; a stale/0 cloud row (xp writes in `addXp`/`awardEndOfMatch` are unawaited, swallowed, and skipped entirely when `sbUser` is null) clobbers good local xp → level 1. Whoever writes last wins, and 0 always wins. Duplicate **profiles** rows are barred at the DB level: `profiles.username citext unique` (sql/full-setup.sql:17 & social-schema.sql:11) + deterministic auth email `username@trenchmon.app` (auth-unique) ⇒ one uid per username. **Cannot enumerate live dupes from the client** (RLS: SELECT only own row; service-role key is Vercel-only, not in repo) — verify via a Supabase SQL-editor query (predicted: 0 dupes). SAFE FIX (await user decision): a `merge_profile` RPC that upserts monotonic fields with `GREATEST(existing, incoming)` so 0 can never lower a real value, race-safe across devices; replace the raw upsert/update + the `||0` pulls with it; (only if dupes are found) merge-keep-highest then dedupe — never blind delete (FKs cascade). No build/badge bump (diagnosis only).

> **🔧 ADMIN GRANT (b86, owner account only — SQL-only, NO code/build/badge change):** one-time dev setup for the owner account `rocky`. **Username `rocky` → `RockyDEV`** (the requested "Rocky DEV" with a space is UNSAFE: the login email is derived `username.toLowerCase()+'@trenchmon.app'`, so a space → invalid `rocky dev@…` email → broken login; used the no-space fallback). **Max level = 6554** (levelFromXp has no hard cap; the real ceiling is the `xp int` column = int4 max 2,147,483,647 → max representable level 6554; exact xp to reach it = **2,147,418,100**). **Coins = 999999** via a deliberate direct `profiles` UPDATE (coins-sacred law UNCHANGED — client still writes coins only through settle_match; this is an out-of-band admin grant, not a client write). Leaderboard: **NOT excluded** (the RPCs aggregate all `match_results` by username — RockyDEV appears once it plays ranked; historical `rocky` rows stay under the old name). Rename requires BOTH `profiles.username` AND the auth email (`auth.users.email` + `auth.identities`, or the Dashboard) to change together, else login breaks (email is username-derived); owner must then log out + sign in online as `RockyDEV` so the b86 max-merge pulls xp/coins down to the device. SQL printed in chat for the user to run; not applied by me. No `index.html`/code touched; merge_profile/xp logic for real players untouched.

> ## ⚠️ END-OF-SESSION RULE — update BEFORE you wrap (this is what stops cross-session drift)
> At every clean stopping point and always before ending a session, update this file:
> 1. **Current build number** (the `## Current build:` line below) — match the badge you bumped in `game_template.html`.
> 2. **Status table** — flip rows to their real status (done / in-flight / next) + build #.
> 3. **Log** — add a one-line `bNN:` entry for what changed + what's next.
>
> Then confirm the badge is bumped and `index.html` is built/validated. (Rules/gotchas change → update CLAUDE.md instead.)

## Current build: **b107** (badge in `game_template.html`, search `based-cup bNN ✓`)
Last verified (b107, THREE contained cleanups — UI corner-icons + local-2P launch/controls + roster check; no netcode/gameplay-tuning change, no IDs renamed/removed). **(1) SOCIAL + STORE = corner ICON buttons.** `#socialBtn` 👥 → top-LEFT (`.cornerIcon{top:60px;left:12px}`, under the `#acctBtn` chip which is top:12/h~42); `#storeBtn` 🛍️ → top-RIGHT in the bell/X row (`{top:12px;right:112px}`; the row is BELL right:12 · X right:62 · STORE right:112 — three 42px circles, even 50px spacing, verified no overlap). Both KEEP their IDs + handlers (`$('socialBtn').onclick=openSocial`, `$('storeBtn').onclick`→store overlay). New `.cornerIcon` class (bell-style circle). They're moved to `#stage` (siblings of acctBtn/bell/xBtn) at z55, toggled with `#xBtn` via a new `setCornerIcons(show)` at the 3 xBtn sites (menu-enter show, in-match hide); overlays (z56) cover them elsewhere, the lobby (`#startScreen` z20) reveals them → visible only on menus/lobby, hidden in-match. Removed the dead `#startScreen #storeBtn` rail-accent rule; widened the portrait `#acctBtn` reserve `calc(100vw-122px)`→`-172px` (extra right icon). **(2) LOCAL 2-PLAYER (desktop only).** `#local2pBtn` "🎮 LOCAL 2P" added to railL; onclick launches `oppActive=true; mode='2p'; practiceOpp='2p'; beginGame()` (the `mode==='2p'` path already existed — controlPlayer handled P2 movement/kick; it was just orphaned when Practice lost its select screen). Hidden on touch (`if(IS_TOUCH) _l2.classList.add('hide')` + onclick `if(IS_TOUCH)return`). **CONTROLS — P1: WASD · SPACE kick · F lunge · ShiftLeft sprint | P2: Arrows · ENTER kick · / (Slash) lunge · ShiftRight sprint; both aim by FACING** (keyboard-only). Changes in `controlPlayer`/`setAim`: P1 aim→movement heading when `mode==='2p'` (was mouse); added P1 SPACE-kick + F-lunge and P2 Slash-lunge (P2 kick=Enter already existed); **fixed a latent shift cross-trigger** — generic `keys['shift']` is set by EITHER shift key, so P2's ShiftRight also sprinted P1; P1 now uses ShiftLeft-only in 2p (single-player still accepts either shift). `keydown` adds `preventDefault` for Space/Enter/'/' while `state==='play'` (no page-scroll / focused-button re-activation). Single-player (mouse aim + mouse kick + right-click lunge), online, and CPU-practice paths are untouched (all new keyboard kick/lunge gated to `mode==='2p'`; the P2 branch only runs in 2p). **(3) ROSTER — pepe + trumpe PRESENT & selectable (no change).** They inject via `__PEPE__`/`__TRUMPE__` from `pepe_cut_s.png` (71,958 B) / `trumpe_cut_s.png` (103,879 B) — both on disk, non-zero (not OneDrive-dehydrated) — which are SEPARATE from build.py's `EXTRA` list; the "Extra characters live: [12]" print is just the extras, not the full roster. `ROSTER` (game_template.html) = `['pepe','elon','pdidpe','obampe','chad','wojak','okeyjak','sadjak','doomer','boomer','soyjak','trumpe','grandpa','oldwoj']` (14, incl. pepe+trumpe) and `STATS` has both; the locker carousel renders `ROSTER`. So 14 total (pepe + trumpe + 12 extras). Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders; 0 console errors on load; each of socialBtn/storeBtn/local2pBtn present exactly once (no dup IDs). ⚠️ The animated-canvas preview is wedged (window reported 0×0), so verified via computed geometry (corner-icon spacing/no-overlap), console-clean load, harness, and a 16/16 control-scheme sim (`sim_2p.cjs`) — recommend a quick human eyeball of the two corner icons + a live local-2P match on desktop.
✅ **b107 = Social/Store corner icons + Local 2P (desktop) restored + roster confirmed (pepe/trumpe fine).** SOCIAL 👥 top-left, STORE 🛍️ top-right (bell/X row, no overlap) — moved out of the button stack, same IDs/handlers, shown on menus/hidden in-match. LOCAL 2P button (desktop-only, hidden on touch) launches the existing `mode==='2p'`: P1 WASD/Space/F/ShiftLeft, P2 Arrows/Enter/Slash(/)/ShiftRight, both aim by facing; fixed the shift cross-trigger; single-player/online/CPU-practice unchanged. Roster: pepe+trumpe inject via separate `__PEPE__`/`__TRUMPE__` tokens (sprites present) and are in ROSTER/STATS — all 14 selectable, the "12" was just the extras printout. UI + control-scheme + launch-path only; IDs preserved (harness passes).
Last verified (b106, RED-CARD / online match-end reliability — netcode only; offline unchanged, no gameplay/stat change, no IDs renamed/removed). **The ask:** a RED card (2nd yellow→red→removal→team at 0→endGame) must END the match and show the standard REMATCH screen, offline AND online, and rematch must actually start a new match; online both peers reach the end/rematch screen with no desync/hang. **OFFLINE (vs CPU): was ALREADY working** — `issueFoul`→`endGame`→`winScreen`+`setupEndScreen()` (REMATCH enabled) is the SAME shared end screen as time-up/goal; `clickRematch` (no netRole) hides the win screen and calls `newGame()` which clears `out`/`cards` (L1759) so the rematch starts clean; the red-card flash `#cardFlash` is `pointer-events:none` so it never blocks REMATCH; 1v1 `teamAlive` correctly hits 0 → `endGame(otherTeam)`. **ONLINE: was BROKEN (guest hang / desync).** ROOT CAUSE (online-only; also affected time-up ends): the host announces the end ONLY via a DC state snapshot with `st:'win'` (`endGame` L2504), but the DC is `createDataChannel('game',{ordered:false,maxRetransmits:0})` (L4256) — UNRELIABLE + UNORDERED — and in the SAME transition frame the host ALSO runs the steady-60Hz `sendState()` at L2962 (endGame flips `state→win` MID-frame: onTimeUp L2941 / processLunges→red L2951, both BEFORE L2962). The guest transitions only on the NEWEST buffered snapshot's `st` (applyNetState L4653), so the trailing `st:'play'` overwrote the win → guest stuck on the play screen (no rematch; host already on the win screen = desync); on a lossy link the lone win snapshot could also just be DROPPED (maxRetransmits:0) → same hang. The match-end is a one-time critical control event but was never on the reliable-WS list (the L4205 rule: start/hello/wagerAck/rematch/left MUST use WS). **FIX (b106, 3 parts):** (1) gate the L2962 trailing send on `state==='play'` so the win snapshot stays the newest DC frame (halftime unaffected — `state` stays `'play'`); (2) `endGame` (host) ALSO sends a reliable WS control msg `NET.relay({end:who})` (routes to WS since it isn't state/input/ping/pong); (3) guest `onRelayData` handles `d.end` → `if(netRole==='guest'&&state==='play'){ state='win'; showWinFromNet(d.end); }`, idempotent with the DC path (both guard `state==='play'`, so whichever lands first wins and the other is a no-op → `recordResult`/`awardEndOfMatch` fire exactly once). Verified via a control-flow sim (`sim_end.cjs`, scratchpad): OLD model is order/loss-dependent (hangs at `'play'` in the common in-order case AND when the win is dropped); NEW model reaches `'win'` deterministically even with the DC win dropped or reordered. Rematch path (`clickRematch`/`doRematchStart`/`sendHostStart`→guest `d.start`) unchanged — same as a normal-end rematch, which this fix also hardens. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders. ⚠️ Real-browser rAF/screenshot historically wedged here — verified by code trace + the deterministic sim.
✅ **b106 = red-card (and all) online match-end made reliable → standard REMATCH screen for both players.** Offline red→rematch was already fine (shared winScreen/setupEndScreen; `newGame()` clears `out`/`cards`). Online was broken: the host announced the win ONLY as a loss-tolerant/UNORDERED DC snapshot, and the same frame's steady-60Hz `sendState()` sent a trailing `st:'play'` that overwrote it (the guest reads only the newest snapshot) → guest hung on the play screen (or the lone win packet was dropped on a lossy link) = desync. FIX: gate the trailing send on `state==='play'`, ALSO send the end over the reliable WS (`NET.relay({end:who})`), and handle `d.end` on the guest idempotently. Netcode-only; offline/gameplay/stats untouched; IDs preserved (harness passes).
Last verified (b105, MAIN-MENU character-card redesign + six ore tiers + sprite shrink — LOBBY showcase card only; visual, no stat/gameplay change, no IDs renamed/removed). **(1) SIX ore bands by OVERALL /120** (`oreTier` rewritten): 🪵Wood 75-79 · 🪨Stone 80-83 · ⚙️Iron 84-87 · 👑Gold 88-90 · 💎Diamond 91-93 · 💀Netherite 94-100 (was 5 bands: stone75/iron80/gold84/diamond88/nether94). New `tierEmoji(c)` maps each band→emoji. Verified across the roster: EPPE75🪵 · WOJAK82🪨 · TRUMPE84⚙️ · PEPE88👑 · CHAD93💎 · ALEX100💀 (all six populated). **(2) MAIN-MENU CARD FRONT restructured + NARROWER:** the `#lobbyCard` is now a PORTRAIT card (aspect `2/1`→`1/1.05`; `#lobbyShowcase` `min(340px,60vw)`→`min(196px,44vw)`; landscape `min(53vh,244px)`→`min(30vh,152px)`) showing NAME + OVR + ore **EMOJI** (tier shown as JUST the emoji — the ore GRADIENT styling was removed from the lobby card, so it's a clean FIFA-white card) + **OFFENSE ★/5** + **DEFENSE ★/5** as real half-star icons (repurposed `lobbyBannerFront`→star rows; kept IDs `lobbyOvr`/`lobbyTierName`(now holds the emoji)/`lobbyBannerStats`; added `#lobbyCardName`). **OFF/DEF derivation** (display-only, `offStars`/`defStars`, half-stars via `roundHalf`): **OFFENSE = (pace+dribbling+power+diving)/16** (avg of the four attacking /20 stats → /5); **DEFENSE = (defending+stamina)/8** (avg of the two /20 stats → /5). Star CSS re-scoped for the lobby (`.loStar` was `#lockerScreen`-only) → renders gold (`--fc-gold`). Samples: PEPE OFF4/DEF3 · EPPE OFF2.5/DEF4.5 · ALEX OFF4.5/DEF3.5 · OLDJAK OFF3.5/DEF5. **(3) CARD BACK** unchanged (`lobbyBannerBack` still reuses `getCharRecord`: W/L, goals F/A, own goals, dribble%, tackle%, cards) — flip still works; back re-laid to a single column for the narrow card. **(4) LIVE SPRITE SHRUNK ~half** (`lobbyAnimDraw` `R=Math.min(H2*0.34,W2*0.20)`→`min(H2*0.185,W2*0.095)`): the head is now ~18% of the pitch width (was ~34%), lots of room to roam; animation intact. **LOCKER:** layout untouched (still detailed stats + OVR, no tier-name text) — only the shared ore-tier BANDS updated to the six (added `.loSlot.tier-wood` warm-timber styling + fixed the now-shifted range comments); so the locker cards recolor to the new bands consistently. Verified in-browser: card 196×206 desktop / 108×113 (740×360) / 113×118 (667×375) portrait, narrower, stars inside the card; front name+OVR+emoji+OFF/DEF stars; flip→record; equip change updates (→EPPE 75 🪵); sprite head 18% of box; no scroll, PLAY reachable, no overlap desktop+landscape; screenshot confirms clean layout; 0 console errors. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b105 = main-menu card redesign + six ore tiers + sprite shrink.** Six emoji tiers (🪵🪨⚙️👑💎💀 by OVR band); the main-menu card is now a narrow FIFA-white PORTRAIT card = NAME + OVR + ore EMOJI + OFFENSE ★/5 + DEFENSE ★/5 (real half-stars; OFF=(pace+drb+pow+div)/16, DEF=(def+sta)/8), back = the fuller record (flip works). Live lobby sprite shrunk ~half (head ~18% of the pitch width) so he has room to roam. Locker keeps its detailed layout; only the shared ore bands were updated (Wood added, re-spaced). Visual only; no stat/gameplay change; IDs preserved (harness passes).
Last verified (b104, GUEST MOBILE MOVEMENT "ankle weight" fix — guest-side reconciliation only; host/opponent/lunge untouched). **ROOT CAUSE:** the guest predicts its OWN player locally (`predictGuestSelf`) — physics + b95 joystick gain are byte-identical to the host's `applyMove` (verified: same ACC/MAX/friction, both read `joyVec`; input was NOT the issue). The drag came from the OWN-player **reconciliation** toward the freshest authoritative snapshot: `k = err>60 ? 0.16 : 0.05`. But the guest's own player LEGITIMATELY leads that snapshot by ≈`v*RTT` — at the ~300ms relay floor (STATE) and `maxOff≈5px/frame` that's **≈90px**, which **exceeds the 60px threshold**, so during NORMAL movement the FIRM 0.16 tier fired and yanked the local prediction back toward the RTT-delayed position. Worst on **direction changes / stops** (where the stale auth opposes the new input): a reversal sim showed the OLD path produce a **6.11px/frame WRONG-WAY shove** (guest jerked right while pressing left) = the sluggish/weighted feel. The LUNGE feels fine because its burst dash (~17px/frame, also relayed + applied host-side) dwarfs the reconciliation pull, so it punches through. (Also confirmed `applyNetState` line ~4652 sets the own player from the interp-delayed sample, but `predictGuestSelf` overwrites it every play frame → not the cause; b84 opponent interp on `players.p`+ball is untouched.) **FIX (one line in `predictGuestSelf`):** `k = err>150 ? 0.16 : 0.04` — the gentle 0.04 lets the LOCAL prediction lead in real time (responsive), and the firm tier now only catches genuine drift (>150px); hard-snap at >200 (goal/kickoff) kept. Reversal sim: worst wrong-way shove 6.11→**2.38** (just momentum), and the guest builds turn-around speed faster (−1.5→−2.0). Steady-state speed unchanged (~full). Runtime-verified: guest prediction moves the own player at full speed with no auth-drag, and `guestPredictLunge` still fires (`gp.lunging=14`); 0 console errors. Lunge / opponent smoothing (b84) / host movement all unaffected (change is inside the guest-only branch). Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b104 = guest movement "ankle weight" fix.** Root cause = the guest's own-player reconciliation firm-tier (`err>60→0.16`) fired during normal high-latency movement (the guest's correct prediction lead ~90px at the 300ms relay floor exceeds 60px), dragging the local prediction back toward the RTT-delayed authoritative position — worst on stops/reversals (a 6.1px/frame wrong-way jerk). The lunge escaped because its burst dwarfs the pull. FIX: raise the firm threshold + soften the gentle tier (`err>150 ? 0.16 : 0.04`) so the local prediction leads in real time; drift/snap protection kept. Guest-only; host/opponent-interp/lunge untouched.
Last verified (b103, MOBILE CHROME CLEANUP — four visual/layout fixes; no gameplay/netcode, no IDs renamed/removed). **FIX 1 — top-left profile chip (`#acctBtn`, z55) covered menu BACK buttons + touched the LOCKER:** ROOT CAUSE = every `.overlay` was `z-index:20` (BELOW the z55 chrome), so on any menu the account chip poked through and covered top-left back buttons (esp. the Locker's round `loBackBtn`). FIX: `.overlay` → **z-index:56** (above the chip), and `#startScreen` pinned to **z-index:20** so the chip still shows on the lobby but every other overlay COVERS it (its own back button on top). Popups stay above (inbox 58, badges 60, serverPopup 71, challengePopup 80). Verified: on the locker the topmost element at the back button is the back button; the chip zone is covered by `lockerScreen`; on the lobby the chip is topmost. Also capped `#acctBtn` width + name-ellipsis in landscape so a long name can't reach the LOCKER rail (already ~40px clear at all sizes; this is a belt). **FIX 2 — BACK buttons more prominent:** `button.btn.ghost` (all BACK/CLOSE) → bigger tap target (`min-height:48px`, `padding:12px 26px`), brighter (`#1d5233` + 2px `#4fae6e` border, light text), press feedback; the Locker's round back → 50×50 (was 42) with a 2px accent border. Consistent + FIFA-clean across screens (desktop too — intended). **FIX 3 — in-match ping to the BOTTOM:** `enterMatchChrome` lifted BOTH the pot AND ping pills to `bottom:156px` on touch (mid-screen on a short landscape phone). Now only the pot is lifted; the **ping** goes to `bottom: calc(safe-area + 6px)` (touch) / `10px` — centre-bottom, between the left joystick and the right KICK cluster, out of the way, still readable. Verified `pingTag` bottom = `10px` after `enterMatchChrome` (not 156). **FIX 4 — landscape less squishy:** more breathing room in the b102 landscape query — grid gap `3px 8px→7px 14px`, bigger side padding, title 17→19, rails 6→9px padding + 5→8 gap, showcase `50vh→53vh` / char `62vh→66vh`, `liveCountMenu`/`lobbyOvr`/stats up a notch. Kept the PLAY-bar controls sized to stay on ONE line at 667px (bigger sizes wrapped it → collided with the guest pill). Verified at **740×360 / 667×375 / 820×360**: still NO scroll, all zones fit, PLAY visible/tappable, one-line play bar, guest pill clears every control (typeSeg/modeSeg/PLAY/railL). Desktop unaffected (z-index chrome correct: chip on lobby, covered on overlays; play zone column; 440px PLAY). Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders; 0 console errors. ⚠️ Screenshot tooling still wedged (animated lobby canvas) — verified via `getBoundingClientRect` + `elementFromPoint` at all three landscape sizes + desktop.
✅ **b103 = mobile chrome cleanup (4 fixes).** (1) Profile chip no longer covers menu back buttons — `.overlay` raised to z56 above the z55 chip, lobby pinned z20 (chip still shows there); acctBtn width-capped in landscape so it never reaches the Locker. (2) BACK buttons bigger + clearer (min-height 48, accent border) across screens; locker round-back 50px. (3) In-match ping moved from mid-screen (156px) to the very bottom-centre, clear of the joystick/KICK. (4) Landscape lobby given more breathing room while still fitting (no scroll, PLAY reachable, play bar stays one line at 667). Desktop untouched; harness passes.
Last verified (b102, MOBILE-LANDSCAPE lobby REFIT + guest SOL prompt close/persist). **ROOT CAUSE of "landscape lobby is oversized / must scroll / PLAY unreachable":** the lobby's only mobile media query is `@media(max-width:640px)` (a PORTRAIT stack that scrolls). Landscape phones are WIDE — 667×375, 740×360, 820×360 all have width **>640**, so they fell through to the **desktop** grid (hero-sized showcase, tall banner, 3 stacked rows) which is built for tall viewports and overflowed the ~360px landscape height. Since the b96 lock now FORCES landscape, that overflow is what every phone user hits. **FIX 1 (landscape refit — new `@media (orientation:landscape) and (max-height:500px)`; desktop >500px tall untouched; portrait lock untouched):** fill the viewport, `overflow:hidden` (no scroll), lay out HORIZONTALLY — a 3-col grid `railL | stage | railR` with a thin title row on top and a horizontal PLAY bar (TYPE + MODE selectors + one PLAY) on the bottom. The showcase (banner + animated char) is sized by **HEIGHT** (`vh`: banner `min(50vh,228px)`, char `min(62vh,286px)`) so it never exceeds the short row; rails compacted (small buttons, ellipsis, menuBoard hidden); title shrunk + sub hidden. Verified at 740×360 / 667×375 / 820×360: **NO scroll, every zone within the viewport, PLAY always visible/tappable, selectors present, nothing clipped/squished.** **FIX 2 (guest SOL prompt):** added an **✕ close button** (`#guestBannerClose`) + a session flag `guestPromptDismissed` — `refreshGuestBanner()` now stays hidden once dismissed (no re-nag on menu re-entry; verified across repeated refreshes). In landscape the prompt becomes a **compact button-pill** (long message hidden via a new `.gbMsg` wrap) docked in the gap **above** the PLAY bar so it clears PLAY, the selectors AND the side rails (the full 2-line card was too big for the short height). Desktop keeps the full card with message, bottom-left. Verified: desktop lobby unchanged (play zone column, 440px PLAY centred, full banner); portrait still shows the rotate lock; 0 console errors. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders. ⚠️ Screenshot tooling still wedged (animated lobby canvas hangs it) — verified via `getBoundingClientRect` geometry (fit / no-scroll / no-overlap) at all three landscape sizes + desktop.
✅ **b102 = mobile-landscape lobby refit + guest prompt close/persist.** Landscape phones (width >640) were getting the desktop layout → oversized, scrolling, PLAY off-screen; added a landscape media query (orientation+max-height) that lays the lobby out horizontally and sizes the showcase by `vh` so it all fits the short viewport with no scroll and PLAY always reachable. Guest SOL prompt gets an ✕ that persists for the session (no re-nag) and a compact landscape pill that clears all controls. Desktop + portrait lock untouched; no gameplay/netcode change.
Last verified (b101, KEEP-AWAY / mode-select RESTORED + lobby bottom rebuilt as SELECTORS + PLAY — implements the b100 diagnosis; fixes the b70/b87 orphaning). **What changed (UI flow + 3 hard-code removals only — NO gameplay/netcode/keep-away-logic change):** (1) the lobby bottom (`#lobbyPlayZone`) is now **TYPE selector** (⚡QUICK / 💰WAGER / 🎮FRIEND, `#lobbyTypeSeg`, keeps the old `lobbyQuick/lobbyWager/lobbyFriend` IDs as its buttons) + **MODE selector** (⚽GOALS / ⏱️KEEP-AWAY, `#lobbyModeSeg`) + a hint + **one PLAY**. Both reuse the existing `.seg` styling + the `seg()` helper (which already sets `gameMode`); selected state highlights green. (2) PLAY dispatches by the selected type: quick→`msQuickBtn.click()`→`startQuickMatch`; wager→`msWagerBtn.click()`→`onlineWagerEnabled`→`openWagerSelect`(stake)→queue; friend→`msFriendBtn.click()`→`openOnlineLobby` — all with `gameMode` already set. (3) **Deleted the 3 `gameMode='goals'` hard-codes** (wager `.wsAmt`, `msQuickBtn`, `practiceBtn`) so Quick/Wager/Practice now respect the lobby MODE selector (`newGame()`/`matchLen()` read `gameMode`; nothing re-resets it). (4) Removed the b91 `lobbyQuick→msQuickBtn` `.click()` map (would have overridden the selector's onclick). (5) `syncLobbyMode()` re-highlights the MODE selector to match `gameMode` on return to the lobby (`gotoMenu`) — so a friend-host keep-away pick is reflected. **HARD SAFETY:** kept `modeSeg`/`selectScreen`/`ms*Btn`/`selStartBtn`/`lobbyQuick/Wager/Friend`/`playBtn` IDs in the DOM; `#modeSelectScreen` hub + `openModeSelect()` stay (hidden/unused) so the launch logic + IDs survive the harness. Friend-host keep-away path (`openSelect('host')`→`#modeSeg`) left intact; online host mode still syncs via `sendHostStart`→`mode`→guest `d.start.mode` (untouched). Verified in-browser: MODE selector sets `gameMode` (matchLen 90/keepaway, 60/goals) + updates the hint; TYPE selector sets `lobbyType` + highlights; PLAY dispatches quick(gameMode=keepaway)/wager/friend correctly; a **keep-away practice match runs** (`_beginGame`→matchLen 90, "KEEP-AWAY · HOLD IT", `onTimeUp` with possP>possT → possession win); layout clean + no overlap desktop(1280: type & mode side-by-side, PLAY centred below) AND mobile(375: blocks stack, PLAY fits); 0 console errors. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b101 = keep-away/mode-select restored + lobby bottom = selectors + PLAY.** Fixes the b70/b87 orphaning: Quick/Wager/Practice hard-coded `gameMode='goals'` so keep-away was only reachable via friend-host. Now the lobby has ⚽GOALS/⏱️KEEP-AWAY + ⚡QUICK/💰WAGER/🎮FRIEND selectors and one PLAY that launches the chosen combo; the 3 hard-codes are gone so keep-away plays in every flow. UI-flow + 3-line removal only — gameplay/netcode/keep-away scoring untouched; IDs preserved (harness passes); online host-mode sync intact.
Last verified (b100, LUNGE "doesn't fire" — REAL ROOT CAUSE via a live real-browser test + Esc-online). **THE ACTUAL PROBLEM (b98/b99 chased the wrong thing — the input was never broken):** I proved the lunge input+mechanic are correct by driving the **live deployed site in a real Chrome browser** and doing a **real trusted right-click** in a real match — `contextmenu` fired + was preventDefault'd, `tryLunge(players.p)` was called, `lungeCD` set, stamina dropped, **Pepe visibly dashed**. What blocks the lunge is `tryLunge`'s guard `if(kickoffLock>0 || goalLock>0 || halftime) return;` — no lunge during the **~0.9s kickoff freeze** (nor goal/foul freeze). The ORIGINAL "worked at b85, broke since" regression = the **pre-b97 card bug** (CPU racked up 50+ yellows, never sent off → fouled CONSTANTLY) kept the match in near-perpetual `foulReset`→`kickoffLock=55`/`goalLock=40`, so the human's lunge was blocked almost always = "does nothing." b97 fixed the fouling; the leftover friction was the dead ~0.9s at every kickoff. **FIX (b100):** dropped `kickoffLock` from the lunge guard in BOTH `tryLunge` (host/offline) and `guestPredictLunge` (online guest) → lunge is available at kickoff (movement already was); **goal/foul/half-time freeze still blocks**. Diving/cooldown/stamina/foul (b85) + b97 cards UNCHANGED. Verified: fires with `kickoffLock=55` (was blocked), still blocked with `goalLock=70`. **ALSO — Esc in ONLINE matches: ROOT CAUSE** `togglePause(){ if(netRole) return; }` hard-disabled pause online (can't freeze a live PvP) → in a QUICK/WAGER match Esc did nothing = "neither does escape." **FIX:** online Esc now opens/closes the in-match MENU overlay ("MENU", Resume/leave) **without** freezing the sim (no desync); offline pauses as before. Verified: online Esc toggles the menu, `state` stays `play`. (Deploy confirmed live at based-cup.vercel.app, b99, `Cache-Control: must-revalidate` → users get fresh HTML on reload; not a stale-cache issue.) Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b100 = the REAL lunge fix (kickoff dead-window) + Esc works online.** PROVED via a live real-browser right-click that the lunge input+mechanic are correct (Pepe dashed); "doesn't fire" was the `kickoffLock` guard blocking it ~0.9s after every kickoff — brutal during the pre-b97 CPU foul-spam that kept the game in perpetual kickoff/goal-lock. b100 drops `kickoffLock` from the lunge guard (host+guest); goal/half-time still blocks; mechanic + b97 cards untouched. Online Esc now opens/closes an in-match MENU (was hard-disabled online). b98/b99 input hardening was defensive, not the cause.
Last verified (b99, TWO fixes — lunge/pause input routing + wager-routing diagnosis). **FIX 1 — right-click = lunge ONLY, menu on ESC. ROOT CAUSE of the flaky right-click / "context menu inconsistently pops":** the `contextmenu` `preventDefault` (and the b98 lunge triggers) were bound to the **`<canvas>` (`cv`) ONLY** — so a right-click that landed anywhere else in the play area (the HUD scorebug, player cards, the pause/sound buttons, the letterbox margins outside the 1100px `#stage`, or the touch layer) was NOT suppressed → the browser's native menu popped AND the lunge didn't fire there. Inconsistent because it depended on the exact pixel hit. **FIX:** replaced the cv-scoped `contextmenu`/`auxclick` handlers with ONE **document-level `contextmenu`** handler that, while `state==='play'||'paused'`, `preventDefault`s (menu never pops anywhere in the play area) and — during `play` — sets `lungeReq` (right-click lunges from anywhere). Right-click now does ONLY the lunge; in menus/lobby the browser menu behaves normally (no over-suppression). Kept the `cv` mousedown(`button===2`) for instant on-canvas response. **MENU/PAUSE was already on `Escape`** (keydown, line ~1711 — `p`||`Escape`→`togglePause` when playing/paused); it is the ONLY `Escape` binding (overlay-closing is button-driven via `hideAllOverlays`, no conflict) — kept it, updated the pause-button tooltip `Pause (P)`→`Pause (Esc)`. Lunge MECHANIC (diving scale, cooldown, stamina, foul detection — b85) and b97 cards UNTOUCHED — input routing only. Verified on built index.html: right-click on the canvas AND on the HUD both lunge + suppress the menu (the off-canvas case was the bug); mousedown-button2 + mobile LUNGE button both lunge; Esc toggles play↔paused; right-click in a menu is NOT suppressed (normal) and doesn't lunge; 0 console errors. **FIX 2 — WAGER routing: DIAGNOSED, code is CORRECT (no change made, per the "report if it just shares UI" guidance).** Traced the whole path: `lobbyWager`→`msWagerBtn`→`openWagerSelect`→pick stake (`stakeSel=N`, `csContext='wager'`)→`startQuickMatch()`. Both wager AND quick send the SAME `{t:'quick'}` to the relay, and the relay (`lobby/server.js`) has a **single FIFO `quickQueue`** that pairs any two waiters. The stake is then negotiated PEER-TO-PEER in the handshake (`sendHostStart` advertises `stakeSel`; `setupGuestWager` takes `min(hostStake, myStake)`; `wagerAck`→`settleWager`). **Verified the stake carries between two wager players** (host advertises 5 → guest negotiates min(5,5)=5 → host finalizes `wager.amount=5`). So wager does the full handshake and stakes coins — it is NOT "wrongly routing to quick with no handshake." The reason it *appears* to be quick: wager & quick share ONE matchmaking queue, so a wager player is often paired with a quick (0-stake) player → `min(N,0)=0` → casual. That's the intended "opportunistic wager rides the existing relay" design (per the wager-handshake comment). Dedicated wager matchmaking would need a SEPARATE relay queue (a `lobby/server.js` change + manual `fly deploy`) and would make wager matches much rarer (smaller pool) — NOT changed here. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b99 = right-click lunge/menu routing fix + wager-routing diagnosis.** FIX 1: root cause = `contextmenu`/lunge were `<canvas>`-scoped, so right-clicks off the canvas (HUD/cards/pause btn/letterbox) popped the native menu and didn't lunge — replaced with a document-level `contextmenu` handler that suppresses the menu across the whole play area and fires the lunge (during `play`); right-click is now lunge-only; menu/pause stays on Esc (tooltip updated); mechanic + b97 cards untouched. FIX 2 (report, no change): wager is NOT mis-coded — it runs the full handshake and stakes coins (verified two-wager-players → stake 5); it shares the relay's single FIFO quick-queue with quick, so it opportunistically produces casual matches vs non-wager opponents. Separating needs a relay `fly deploy` (+ starves the wager pool) — left as-is per guidance.
Last verified (b98, LUNGE INPUT ROBUSTNESS — right-click dive/lunge reported dead on desktop). **DIAGNOSIS:** the lunge input chain — `cv` mousedown(`e.button===2`)→`lungeReq=true` → `controlPlayer(players.p)` consumes it → `tryLunge` — is **byte-identical to b85** (git diff confirms; the b90–b96 lobby/mobile builds + the b97 card fix never touched these lines), and it fires correctly in every reproducible test: single-frame dispatch, full input→`lungeReq`→`controlPlayer`→`tryLunge` chain, AND driving the real `loop()` for 160 frames (right-click at frame 90 → `players.p.lunging=13`). Nothing covers the canvas on desktop (elementFromPoint stack = `cv`→stage→wrap→body; `cv` `pointer-events:auto`), the `contextmenu` is prevented, and the b96 landscape-lock (`#wrap pointer-events:none`) is inert on desktop (`IS_TOUCH=false`). So no code regression was reproducible in the (rAF- + screenshot-wedged, backgrounded) headless preview. **ROOT-CAUSE (fragility, not a diff):** right-click relied **solely on a right-button `mousedown` (`e.button===2`)** reaching the page — which some desktop browser/OS/device combinations don't deliver as a usable page event (context-menu-on-mousedown platforms, trusted-event handling, hybrid-touch quirks), leaving `lungeReq` unset while KEYBOARD movement still works (hence "moves but won't lunge"). **FIX (input only — mechanic/tuning + b97 card logic untouched):** the lunge now also fires from the **`contextmenu`** event (the canonical right-click signal that ALWAYS fires; gated to `state==='play'`, menu still `preventDefault`ed) and from **`auxclick` (button 2)** — three independent right-click signals, all setting the same idempotent `lungeReq` (consumed once/frame → no double-lunge). **Mobile LUNGE button unchanged and confirmed working** (`touchstart`→`lungeReq`; b95 `#sprintBtn` sits clear of it — from the right edge: KICK 22–138, LUNGE 142–228, SPRINT 254–338, no overlap). Verified on the built index.html: all 4 paths (mousedown/auxclick/contextmenu/mobile-button) → `lunging=13`; cooldown gate blocks an immediate 2nd lunge (`lungeCD=71`); stamina drops ≈`LUNGE_STAMINA` (0.154); diving-scaled reach + foul detection (b85) + b97 cards all unchanged; context menu still suppressed; 0 console errors. Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholders.
✅ **b98 = lunge input robustness (right-click dive restored/hardened).** The b85 input code was unchanged and works in every reproducible test, but relying solely on a right-button `mousedown` is fragile across desktop browsers/devices — so the lunge now ALSO fires on `contextmenu` (canonical right-click, always fires) and `auxclick`, guaranteeing right-click lunges (menu still suppressed). Mobile LUNGE button confirmed working. Mechanic/tuning + b97 card logic untouched. Input-only change.
Last verified (b97, REGRESSION FIX — CPU racking up 50+ yellows / never sent off; constant bad tackles). **ROOT CAUSE (a card-persistence bug latent since b85, exposed by b89's more-active CPU lunging — NOT introduced by the b90–b96 lobby builds; git confirms none touched the foul/AI path):** yellow/red counts live on the per-player object (`pl.cards`), but the **goal-kickoff `reset()` (scoreGoal) and the half-time `reset()` rebuild `players` via `mkPlayer` (`cards:0`) and — unlike `foulReset` — never re-stamped the counts**, so **every goal (and half-time) wiped a booked player's yellow count back to 0**. The "2nd yellow → red → removal" threshold was therefore only reachable if a player fouled twice with NO goal in between; in a chaotic match the count kept resetting, so a player accumulated unlimited yellows and was never sent off. It also defeated b89's "already-on-a-yellow → be far more cautious" gate (the CPU was rarely *seen* as on a yellow), so the CPU kept diving in → the "constant bad tackles". Deterministic repro (screenshots + real-time rAF are wedged in this backgrounded preview, so verified via in-page `getImageData`/unit sims): yellow → **goal `reset()` → cards 1→0 (WIPED)** → next foul read as a fresh yellow, never the red. **FIX (foul/card only; no netcode/gameplay/tuning change):** `reset()` now **PRESERVES** each player's `cards` + sent-off `out` across the rebuild (captures `prev=players`, restamps after `mkPlayer`), and **`newGame()` is the one place that clears them** for a fresh match (the online avatar-rebuild also carries them). **The debounce was already correct** — `checkLungeFoul` sets `lungeFoulDone` + the foul reset rebuilds the (non-lunging) players, and `issueFoul` early-returns on `goalLock>0`, so one incident = exactly +1 (verified: 3 processLunges frames over one contact = Δ1 foul). Verified after the fix: yellow → GOAL reset → **cards PERSIST (1)** → 2nd foul → **cards=2 → RED → `out=true` → `state='win'`** (removal fires; count can never exceed 2); `newGame` clears carryover (all cards/out→0); human foul path identical (same `issueFoul`). b89 AI gate confirmed ACTIVE (real `aiControl`×600: foul-risk lunges easy 2 / normal 4 / **hard 0**; clean-tackle lunges easy 9 / normal 21 / hard 49 → CPU rarely dives a foul, lunges only on winnable balls). Build clean; `node --check` OK; harness passes; 0 tokens; 0 placeholder sprites.
✅ **b97 = foul-card regression fix: cards now persist across goal/half-time kickoffs.** Root cause = goal-kickoff & half-time `reset()` rebuilt players with `cards:0` and never re-stamped the yellow/red counts (latent since b85; surfaced by b89's livelier CPU) → every goal wiped the count → the 2-yellow→red→removal threshold was unreachable → 50+ yellows, never sent off. Fix: `reset()` preserves `cards`+`out` across the rebuild; `newGame()` clears them for a new match. One foul still = +1 (debounce intact); 2 yellows now reliably = red = removal (count can't exceed 2); b89 CPU-foul gating confirmed active. No netcode/gameplay/AI-tuning change.
Last verified (b96, LOBBY POLISH — five tweaks, LOBBY/MENU-only except the global landscape-lock; no gameplay/netcode/in-match-sprite/hitbox/grab change; no IDs renamed/removed → harness passes). **(1) FACE CROP → GROUND-LINE (replaces the b94 ellipse mask, which clipped too low to do anything):** `laDrawHead` now draws each head then hard-clips everything BELOW a horizontal **pitch line** at `cutY = headCentre + LOBBY_GROUND_CUT*R` (canvas-space rect clip, so a backflip doesn't spin the cut), with a `R*0.20` destination-out **feather** so the neck fades softly into the pitch (he "rises out of the pitch"). The line **follows the head's vertical bob** so a hop/celebrate never re-exposes a shoulder. **`LOBBY_GROUND_CUT=0.50`** — from a per-sprite silhouette analysis (opaque-width profile via `getImageData`, screenshots were wedged again) the shoulder-flare onset is: pdidpe 0.29 · elon 0.44 · boomer 0.50 · obampe 0.52 · soyjak 0.63 · grandpa 0.65 · oldwoj 0.71 · trumpe 0.86, and the other 6 (chad/doomer/okeyjak/pepe/sadjak/wojak) are round heads with no flare. So **0.50 gives a clean jaw/neck line with shoulders hidden for ~12/14**; the two widest suit sprites (**pdidpe**, and a hair of **elon**) have shoulders drawn hugging the jaw, so a single horizontal line can't fully remove them — they show a small **feathered collar hint** (still a big improvement over b94's full shoulders). ⚠️ If that hint bothers, either nudge `LOBBY_GROUND_CUT` up (hides more, shows less face) or a future per-char cut / source re-cut of pdidpe/elon is the clean fix. Real in-match sprites + grab/hitbox UNTOUCHED (display clip on the lobby canvas only); old `LOBBY_HEAD`/`LOBBY_HEAD_DEF` ellipse config removed. **(2) ENDLESS ROAM:** roam target range widened to **-0.05..1.05** (was 0.16..0.84) so he runs partway **off both edges** (canvas clips) — no visible box wall / hard stop. Verified sim: hx spans -0.04..1.01. **(3) LOWER JUGGLE:** the keepie-uppie header contact dropped from `hy-0.14` to `hy-LOBBY_JUGGLE_LOW` (**0.02**) so the ball dips to nearly the forehead before heading back up = a deeper down-arc (verified sim: ball reaches 0.69·R below head centre). **(4) PHANTOM WALLET PROMPT → BOTTOM-LEFT:** `#guestBanner` moved from a centred top banner to a **FIFA-clean bottom-left card** (`position:absolute;left:14px;bottom:14px`, light `--fc-card` surface, navy CTA; mobile 10px/250px) — the `guestAddWallet` handler is unchanged (connect still works). **(5) LANDSCAPE LOCK (GLOBAL):** `checkOrient` now toggles `body.show-rotate` on `IS_TOUCH && portrait` (dropped the `playing` gate) so portrait is blocked **everywhere (lobby + in-match)**; `#rotateHint` restyled to a clean full-screen "ROTATE TO LANDSCAPE" lock (z-index 600) and `body.show-rotate #wrap{pointer-events:none}` hard-blocks all play/menu touches until landscape. Desktop (`IS_TOUCH=false`) is unaffected. Verified in-browser: 0 console errors; guest banner bottom-left + handler intact; lock shows/blocks and clears; roam + ball-arc sims as above; build clean, 0 tokens, 0 placeholder sprites, harness passes. Lobby animation stays menu-only + light. **STILL PENDING (separate builds):** global in-match face-crop; Social/Store → Fortnite corner-icons.
✅ **b96 = lobby polish (5 tweaks).** Ground-line face crop (`LOBBY_GROUND_CUT=0.50`, feathered, follows the bob — head-only for ~12/14; pdidpe/elon show a soft collar hint a horizontal line can't fully avoid) · endless roam (-0.05..1.05, runs off-edge) · lower/deeper juggle (`LOBBY_JUGGLE_LOW=0.02`) · Phantom prompt → FIFA-clean bottom-left (handler kept) · GLOBAL enforced landscape lock (portrait blocked lobby + in-match, styled, `#wrap` pointer-events:none). Lobby/menu-only except the lock; no gameplay/netcode/in-match-sprite/grab change; no IDs renamed/removed; harness passes. Pending: global in-match face-crop + Social/Store corner-icons.
Last verified (b95, MOBILE controls fix — "concrete" movement + accidental sprint): two touch-only in-match fixes (desktop/keyboard + gameplay speed VALUES unchanged). **ROOT CAUSE of the "walking in concrete":** the joystick output `joyVec` was the raw analog tilt, so a partial push gave partial input → the accel model (v+=ax, cap at base MAX, ×0.84 friction) only reaches full normal speed near ~0.8 tilt — but the OLD rim-sprint (`touchSprint = tilt>0.9`) fired at 0.9, so the only way to move fast was to hit the rim, which also sprinted. No comfortable "full normal run" band existed. **FIX 1 (speed):** `joyXY` now outputs `unit-direction × gained-magnitude` where the magnitude ramps from a deadzone to FULL (1.0) by a moderate tilt — so a normal push already gives full normal speed, same as a full keyboard press. Verified: full-tilt joystick terminal speed = **5.12 = keyboard full** (identical), partial 0.35-tilt = 2.26 (fine control near centre), no sludge. Tunables: `TOUCH_MOVE_DEADZONE=0.16`, `TOUCH_MOVE_FULLTILT=0.60`. **FIX 2 (sprint):** removed the rim-of-joystick sprint trigger entirely (`joyXY` no longer sets `touchSprint`; `joyRelease` no longer clears it) → the joystick NEVER sprints on its own. Added a **dedicated SPRINT button** (`#sprintBtn`, "⚡RUN", gold) to the touch chrome — HOLD to sprint, release to stop; costs stamina as before (unchanged applyMove path). **Placement:** bottom-right cluster, to the LEFT of LUNGE (order across the bottom-right: SPRINT · LUNGE · KICK), right-thumb-reachable, clear of the joystick (left thumb) and not overlapping KICK/LUNGE — verified at 760×380 landscape: SPRINT x[478,548], LUNGE x[564,636], KICK x[640,738], 0 overlaps, all on-screen; hold→`touchSprint=true`, release→false; sprint boosts (6.56 vs normal 5.12) and drains stamina (1.0→0.56). Kept: one-finger joystick binding, aim-follows-joystick, KICK + LUNGE. Desktop untouched (keyboard Shift-sprint; touch layer stays `display:none` off-touch). Build clean; 0 tokens; harness passes.
✅ **b95 = mobile controls fix.** Full-tilt joystick now = full normal speed (deadzone→full ramp; fixes the "concrete" feel); removed the accidental rim-sprint and added a dedicated held SPRINT button (⚡RUN, bottom-right left-of-LUNGE, no overlap, costs stamina). Touch-only — desktop/keyboard + gameplay speed values unchanged. Tunables: `TOUCH_MOVE_DEADZONE`, `TOUCH_MOVE_FULLTILT`.
Last verified (b94, lobby polish — free-roam + lobby-only face crop + slower transitions + deeper ball arc): four LOBBY/MENU-ONLY tweaks to the b93 living-character showcase (no gameplay/netcode/in-match-sprite/grab-radius change). **(1) FREE-ROAMING:** removed the boxed character container — the stats banner + invite now sit in a top row (`#lobbyTopRow`), and the character runs FREE across a wide, borderless `#lobbyCharBox` (spans the stage, subtle ground-glow horizon, no border/bg). He picks a random roam spot each jog/dribble and runs there (roams ~40% of the width), juggles/celebrates in place; faces his run direction. **(2) LOBBY-ONLY FACE CROP:** the displayed head is masked to a **feathered ELLIPSE** around the face (`laDrawHead`, radii × head-R, per-char `LOBBY_HEAD` config) — keeps skin/face/hair/facial-hair + small accessories (Elon's wires), clips the shoulders/shirt, soft rim so no hard/choppy cut. This is a **display-only canvas mask** — the real in-match sprite files + hitbox/grab radius are UNTOUCHED (a later build applies a crop globally). **(3) SLOWER TRANSITIONS:** the pose ease is now the tunable `LOBBY_EASE=0.10` (was a hard 0.18) → smoother move-to-move blends. **(4) DEEPER BALL ARC:** the ball now has real gravity physics (`LOBBY_BALL_GRAV`, `LOBBY_BALL_BOUNCE`) — juggles up to the top and falls to the ground (verified arc depth ≈0.73 of the box, 15 bounces over 15s), not a shallow float; keepups end on a backflip. Still menu-only (self-stops off the lobby) + light. **Tunables:** `LOBBY_EASE`, `LOBBY_ROAM_EASE`, `LOBBY_BALL_GRAV`, `LOBBY_BALL_BOUNCE`, `LOBBY_HEAD_DEF`/`LOBBY_HEAD` (per-char ellipse). Verified (pixel + geometry + deterministic sim — the preview SCREENSHOT tool was wedged this session, so visual spot-check was via canvas `getImageData`): the ellipse crop removes shoulders for all 14 (neck widths dropped from ~2× the face to ~1×; heads centred + consistently sized; feathered rim = no choppy cut), free-roam headX range 0.41, ball arc depth 0.73, all 4 moves cycle, LOBBY_EASE 0.10; no lobby overlaps desktop(1280) + mobile(375, incl. free-roam char), nothing clipped, banner-flip/Locker/PLAY all work, 0 console errors. Build clean; 0 tokens; harness passes; no IDs renamed/removed. **Global face-crop (in-match) is a FUTURE build.** ⚠️ Recommend a quick human eyeball of the 14 lobby heads on the live deploy (screenshot tooling was down) — tune `LOBBY_HEAD[<char>]` if any head clips hair or shows a shoulder.
✅ **b94 = lobby polish: free-roaming character + lobby-only feathered-ellipse face crop + slower transitions + deeper gravity ball arc.** All menu-only; in-match sprites + hitbox/grab radius UNCHANGED (crop is a display mask). Tunables exposed. Global face-crop deferred to a future build.
Last verified (b93, lobby showcase rebuild — flippable stats banner + LIVING animated character): restructured the lobby centre from "sprite in front of card" into a top-to-bottom composition: (1) a **flippable STATS BANNER** (the `#lobbyCard`, now a 2:1 banner) — **tap/click to flip** front↔back; **display-only** (equipping stays in the Locker); reads the equipped char + updates on equip change (resets to front). **FRONT** = OVR (big) + ore-tier styling + tier name + the key mechanic stats (GRAB radius, PACE, DEF, STA, DRIB, POW, DIV) as mini-bars, **no portrait**. **BACK** = the player's record with that char, **reusing the Locker card-back store** (`getCharRecord`): W/L, goals F/A, own goals, dribble%, tackle%, + yellow/red cards. (2) a **LIVING CHARACTER** below in its own mini-pitch box (`#lobbyCharBox` + `#lobbyCharCv` canvas), aligned to the banner width — the equipped head sprite is **procedurally animated** (MENU-ONLY) cycling randomly through **jog / dribble-with-ball / keepie-uppies→backflip landing / celebrate** with **smooth transitions** (ease-actual-toward-target pose; rotation continuous so moves blend). Light + efficient (clear + ~2 draws/frame); the loop **self-stops when the lobby isn't visible** (never runs during a match / burns battery) and restarts on return. (3) PLAY + mode chips unchanged bottom-centre. Reuses the FIFA-clean tokens + ore-tier gradients (front-face) + the full-screen pitch backdrop. **HARD SAFETY:** no element IDs renamed/removed/rewired (kept `#lobbyCard/#lobbyOvr/#lobbyHero`(hidden)/`#lobbyHeroName`; added new ids — ids.json auto-regens); harness passes. Verified desktop + mobile (375px): banner shows OVR 100/NETHERITE + 7 stats incl. GRAB 48.5, flips to the record (W/L 4/1, dribble 67%, cards 🟨2🟥1); character animates (jog/dribble w/ ball/keepups+flip/celebrate), equip-change updates banner+animation (→EPPE/75/STONE), counter clear of the character, scrolls on mobile, menu-only stop/restart works, PLAY+Locker work, 0 console errors. Build clean; 0 tokens; harness passes. **Social/store corner-icon move is a SEPARATE build (still pending).**
✅ **b93 = lobby showcase rebuild: flippable stats banner + living animated character.** Banner (front=mechanics incl. grab radius + OVR/tier, back=your record reusing the locker store) flips on tap, display-only. Below it a procedurally-animated equipped character (jog/dribble/keepups→backflip/celebrate, smooth transitions) on a menu-only canvas that self-stops off the lobby (mobile-performant). No gameplay/netcode change; no IDs renamed/removed; harness passes. Social/store corner icons still pending.
Last verified (b92, MOBILE lobby overlap fix): the b91 full-screen lobby compressed on short/narrow phones — the CSS-grid stage row (`minmax(0,1fr)`) shrank below the fixed-height showcase, so the hero **overflowed up into the live counter** (the reported bug: "players playing now" behind the character's head) and **down into the rail buttons**, and the invite slot clipped the rails. ROOT CAUSE = forcing the whole lobby into a fixed viewport height on screens too short for a hero-sized showcase. **FIX (layout/CSS only, mobile ≤640px; desktop grid untouched):** the mobile lobby now **SCROLLS** — `#startScreen{overflow-y:auto}`, `#lobbyRoot{position:relative;min-height:100%}` with the stage row `minmax(min-content,1fr)` so the showcase **never compresses/overflows**, and `#lobbyBackdrop{position:fixed}` (verified no transformed ancestor) so the pitch stays full-screen behind the scroll. Each zone (counter, showcase-unit, invite, rails, PLAY+chips) now has its OWN row/space → no compression overlaps. The **showcase layering is intact and intended** (hero in front of the ore-tier card = ONE unit, not de-overlapped). Also: extra bottom padding clears the region dock + version badge; on very narrow (~320px) the **account chip is capped (`max-width`) + name ellipsis** so it no longer collides with the X/bell. **Per the owner: the DAILY left-tab and the region/Sydney pill are transparent/casual enough to sit near content — left as-is (not bugs).** Verified in-browser at 375px AND 320px: the live counter is fully visible, NOT behind the character; showcase↔rails / invite↔rails / counter↔showcase overlaps all gone; no buttons clipped off-screen; desktop layout unchanged (no scroll, counter above showcase); 0 console errors. Build clean; 0 tokens; harness passes; no IDs renamed/removed/rewired.
✅ **b92 = mobile lobby overlap fix.** The full-screen lobby now scrolls on phones so every element (counter, showcase, invite, rails, PLAY/mode chips) has its own space — the live counter is no longer behind the character (the reported bug); showcase no longer overflows into the counter/rails; account chip capped so it clears the X/bell on tiny screens. Layout/CSS only, mobile-scoped; desktop untouched; the intended hero-in-front-of-card showcase layering preserved.
Last verified (b91, UI REMODEL phase 1 REBUILD — start menu → FULL-SCREEN Fortnite-style lobby): b90's lobby was a small floating card; b91 is a LAYOUT REBUILD of `#startScreen` into a full-bleed, edge-to-edge lobby that uses the entire viewport (no floating panel). Visual only — no gameplay/netcode/stats/locker/other-screen changes; scoped to `#startScreen`; reuses the Locker design system (`--fc-*`, Oswald, ore-tier card styling). **Structure**: `#startScreen` is now `display:block;overflow:hidden` full-bleed with a **swappable full-screen backdrop** (`#lobbyBackdrop` — a CSS stadium/pitch: perspective stripes + stadium gradient + vignette; **reskin later by changing that ONE rule / its `data-backdrop`**), and a CSS-grid `#lobbyRoot` (`[top] / [railL | stage | railR] / [play]`). **Centre**: the equipped character is **hero-sized front & centre** (`#lobbyHero` ~96% of a `min(340px,80vw)` stage) with their **ore-tier holo card floating behind** (`#lobbyCard`, glow per overall band, OVR badge, name chip) — reads the equip, updates on change (`renderLobbyShowcase`). **Bottom-centre PRIMARY zone**: big navy `PLAY` (opens the mode hub, unchanged) + a `QUICK/WAGER/FRIEND` mode-chip row that reuse the EXISTING hub handlers via `.click()` (no rewire). **Invite slot** beside the character (Fortnite squad slot, hover/tap reveals INVITE → "🤝 coming with 2v2" pill — placeholder, no real party). **Edge rails**: left = Locker/Practice/Store, right = Social/Leaderboard + the Top-Players board; Profile stays on the top-left account chip. Live counter → green pill; version badge kept (bottom-right). Buttons are white FIFA-clean surfaces that pop on the dark stadium; text on the backdrop is light. **Mobile-first**: on ≤640px the grid stacks (title / big showcase / 2-col rails with Locker full-width / play+chips), showcase+invite fit side-by-side, extra top-padding clears the account chip, controls legend + top-players panel hidden. **HARD SAFETY**: no element IDs renamed/removed, no handlers rewired (ids.json auto-regens new IDs) — harness passes; verified Locker/Store/PLAY/invite all still work. Verified in-browser desktop + mobile (375px): full-bleed lobby fills the viewport, backdrop fills the screen, showcase reflects the equip (ALEX netherite/100, PEPE diamond/88), invite reveal + placeholder work, mode chips wired, 0 console errors. Build clean; 0 tokens; harness passes.
✅ **b91 = UI remodel phase 1 REBUILD: full-screen Fortnite lobby.** `#startScreen` rebuilt full-bleed (no floating card): swappable full-screen pitch backdrop, hero-sized equipped character front-&-centre with ore-tier card behind, PLAY + mode chips bottom-centre, invite slot beside the character (2v2 PLACEHOLDER), other actions on edge rails, mobile-first stacked layout. Scoped to `#startScreen`, reuses the Locker design system; no IDs renamed/removed (harness passes); locker/gameplay/netcode/stats/other screens untouched.
Last verified (b90, UI REMODEL phase 1 — START MENU → FIFA-clean Fortnite-style LOBBY): a VISUAL reskin of `#startScreen` only (no gameplay/netcode/stats/locker/other-screen changes). Reuses the Locker design system (the `--fc-*` tokens, Oswald font, navy CTA, framed pills, and the ore-tier card styling Stone/Iron/Gold/Diamond/Netherite) — all SCOPED to `#startScreen` so the Locker + every other screen are untouched. **Central character SHOWCASE**: the equipped character's **sprite in FRONT** of their **ore-tier holographic card BEHIND** (the card glows per the character's OVERALL band, OVR badge top-left, sheen overlay, name chip below) — the hero "steps out" of the card. New `renderLobbyShowcase()` reads the equipped value and repaints on lobby entry + on every equip change (hooked into `updateLockerBtn`). **INVITE slot** (one Fortnite-style empty squad slot beside the character): hover (desktop) / tap (mobile, `.reveal` toggle) surfaces a navy INVITE button; clicking it shows a clean styled "🤝 Invite — coming with 2v2" pill (`showInviteMsg`, self-hiding, menu-safe) and does nothing else — a polished PLACEHOLDER (real party system ships with 2v2). **Buttons restyled** FIFA-clean: white-surface secondaries with role-colour left accents (Locker=green, Store=purple, Social=blue, Leaderboard=gold, Practice=grey) + the navy `PLAY` primary CTA; live-counter restyled to a green pill; menu board / controls panel / guest banner all re-skinned light to match (no unstyled text). **Mobile-first**: showcase + invite + buttons scale on a phone (≤430px shrinks the showcase/slot; card top-padding clears the account chip + bell). **HARD SAFETY**: no element IDs renamed/removed, no handlers rewired — `ids.json` auto-regenerates new IDs; harness passes (no missing-element wiring bug); verified PLAY still opens the mode hub. Verified in-browser desktop + mobile (375px): showcase reflects the equip (ALEX→netherite/100, PEPE→diamond/88, EPPE→stone/75); invite reveal + placeholder message work; buttons styled + functional; 0 console errors. Build clean; 0 tokens; harness passes.
✅ **b90 = UI remodel phase 1: start menu → FIFA-clean lobby.** Equipped-character showcase (sprite in front of their ore-tier holo card), one invite slot (2v2 PLACEHOLDER — "coming with 2v2"), FIFA-clean restyled buttons/counter/board, mobile-first. Scoped to `#startScreen`, reuses the Locker design system; locker/gameplay/netcode/stats/other screens untouched; no IDs renamed/removed (harness passes).
Last verified (b89, AI lunge DECISION-MAKING — the CPU stops fouling itself out): the b85 CPU lunged at a flat per-frame chance whenever the ball was in range, REGARDLESS of foul risk → it dove into the player's body and racked up yellows/reds. **b89 gives the AI reasoning** (decision-only — the lunge MECHANIC, human lunging, and the foul/card system are all UNCHANGED): before lunging, the AI estimates **ball-first (legal, winnable) vs player-first (foul)** using the SAME ball-vs-body comparison the foul check uses, plus the mechanic's own reach math. Logic in `aiControl`: compute `reach=(LUNGE_DIST_BASE+dive*LUNGE_DIST_PER_DIV)*LUNGE_DEF_BONUS` (defender reach, same formula as `tryLunge`); **(1)** `canReach = dBall ≤ reach + LUNGE_AI_REACH_MARGIN` — never lunge out of range; **(2)** `clean = (dOpp−dBall) ≥ marginNeeded` — only lunge when the ball is clearly closer than the carrier's body (ball-first); an AI already **on a yellow** demands `marginNeeded × LUNGE_AI_YELLOW_CAUTION` (much more cautious — avoids a 2nd-yellow sending-off); **(3)** willingness `= D.lunge × clamp(dive/LUNGE_AI_DIVE_REF, .3, 2)` so **high-diving defenders lunge readily, low-diving rarely**; **(4)** if the read is CLEAN it lunges at `willing`, if it's a FOUL risk only a **sloppy (easy) AI** occasionally dives in (`willing × D.lungeSloppy`) — **hard never does**, and **nobody dives a foul while on a yellow**; **(5)** cooldown/stamina still gate inside `tryLunge`. Tiers (distinct): `lunge` willingness easy .012 / normal .040 / hard .075; `lungeSloppy` (dive-in-anyway on a foul read) easy .60 / normal .20 / **hard .00**. Verified in-browser: clean read → lunges (all tiers); player-first read → **hard 0% (never fouls)**, easy ~1% (sloppy); out-of-range → no lunge; a yellow-carded AI with a margin that's clean for a fresh AI does NOT lunge (caution works); per-frame clean-lunge rate easy 1% vs hard 7% (decisive vs rare); diving scales willingness (OLDJAK div18 6% > TRUMPE div12 4% > EPPE div9 3%). Human lunge + mechanic + foul/card system untouched. Build clean; 0 tokens; harness passes; 0 console errors.
✅ **b89 = AI lunge decision-making (CPU rarely cards itself out).** The CPU now lunges only when it reads a clean, reachable ball-first tackle (reuses the foul check's ball-vs-body test + the mechanic's reach math); cautious on a yellow; willingness scales with the DIVING stat; per-difficulty (easy sloppy/rare, hard decisive/clean, hard never dives a foul). Decision-only — lunge mechanic, human lunging, and foul/card system unchanged. Tunables: `LUNGE_AI_REACH_MARGIN`, `LUNGE_AI_FOUL_MARGIN`, `LUNGE_AI_YELLOW_CAUTION`, `LUNGE_AI_DIVE_REF`, per-tier `D.lunge`/`D.lungeSloppy`.
Last verified (b88, per-character stat TRACKING — Locker BUILD 2 extended with CARDS + profile totals + monotonic storage): builds on the b72 observe-only tracking (dribble/tackle/goals/W-L) by ADDING **yellow/red card tracking**, **profile card totals**, and **monotonic Supabase storage** (so a stale/empty context can never wipe accumulated stats — same principle as b86 merge_profile). **What's tracked per character** (stored in `statBlank`): wins, losses, gf, ga, og, dribbleOk/Try, tackleOk/Try, **yellow, red**. **Dribble/tackle** = the existing steal CONTEST (`recordContest`): one pressure event feeds BOTH — not-stolen → carrier dribble success + defender tackle fail; stolen → defender tackle success + carrier dribble fail (numerator+denominator both). The task's "keep possession OR goal-side OR score" reduces to "the defender did NOT win the ball this contest," which the existing not-stolen flag already captures (no gameplay change). **Goals** via `recordGoalStat` (gf/ga + own-goal when the conceding side was `ball.shooter`). **CARDS** via `recordCard`, hooked into the b85 `issueFoul`: **every foul = +1 yellow; a 2nd-yellow sending-off also = +1 red** (cards NOT consumed) → a two-yellow red records **2🟨 + 1🟥** (verified). **W/L** on match end (`commitMatchStats`). **Attribution**: per-match accumulator runs on the AUTHORITATIVE side only (host + offline); at end it commits `matchStat[localSide]` to the equipped char, and the host RELAYS the guest its own deltas (`{charStats}`) which the guest commits once → no double-count. **Storage (monotonic)**: `commitCharStats` writes local then mirrors via a new **`merge_char_stats` RPC** (deep per-field GREATEST); `syncCharStatsFromCloud` now MERGES (max) local↔cloud instead of overwriting (new JS `mergeCharStats`) and pushes the merged-up result — neither side can wipe the other. **Profile screen**: two new styled `.statLine` rows show **TOTAL 🟨 + 🟥 across ALL characters** (`cardTotals()`), matching the game look. **Card backs** (b70) read the same store (real W/L, gf/ga, og, dribble%, tackle%); b75 stars stay inherent-rating (unchanged). **Mode decision**: track every real match WITH an opponent — quick/wager/friend/practice-vs-CPU/local-2P (`statTracking=oppActive`); solo (no opponent) excluded (kept from b72; matches the recommendation). Verified in-browser: a simulated match incremented dribble 2/2 + tackle 1/1, gf/ga/og, **2🟨+1🟥**, W/L on forfeit; committed to the equipped char (pepe); profile showed 🟨2/🟥1 styled; monotonic merge keeps higher values (empty/stale never wipes); 0 console errors. Build clean; 0 tokens; harness passes. ⚠️ **NEEDS (one-time, user): run the `merge_char_stats` SQL in Supabase** (printed in chat; also in `supabase-schema.sql`). Until then the client keeps per-char stats LOCAL (never wiped) and the cloud mirror no-ops; after it's applied, cross-device sync resumes monotonically. (The `char_stats` jsonb column already exists from b70/b72.)
✅ **b88 = per-character stat tracking BUILD 2 (cards + profile totals + monotonic storage).** Adds yellow/red tracking (2🟨+1🟥 rule) hooked into the b85 foul system; profile shows total cards across all chars (styled); per-char stats mirror to Supabase via the monotonic `merge_char_stats` RPC (GREATEST) — stale/empty can't wipe. Observe-only: no change to gameplay/steal contest/foul system/grabCap/coins. **⚠️ Requires the `merge_char_stats` SQL run once in Supabase.**
Last verified (b87, five contained UI/logic fixes — no gameplay/stats/grabCap/netcode touched): **FIX 1 — Practice uses the equipped character:** `practiceBtn` now launches **straight into a vs-CPU practice** with the locker-equipped char (like quick/wager/friend), bypassing the select screen entirely (verified: click → loading, no `selectScreen`, `humanChar`=equipped MUSKPE). *(Side effect: the old practice opponent toggle — solo / 2P-local — was only reachable on that screen and is now bypassed; vs-CPU at the current difficulty. Flag if those should be relocated.)* **FIX 2 — per-card flip:** the locker flip was already per-card in source (verified: 1 card flips, resets on navigate/swipe; screenshot confirmed neighbors stay front-side) — added an explicit flip-reset in `closeLocker()` so flips also clear on **leaving** the locker (verified 1→0). **FIX 3 — carousel order (display-only, computed from data, zero stat change):** `ROSTER` now sorts all "-pe" enders first then all "-jak" enders (each by OVERALL desc), with ALEX pinned last; CHAD (neither suffix) sorts into an "other" group just before ALEX. Verified: MUSKPE90·PEPE88·PDIDPE86·OBAMPE84·TRUMPE84·EPPE75 → OLDJAK94·RUGGEDJAK83·WOJAK82·SOYJAK81·OKEYJAK79·SADJAK78 → CHAD93 → ALEX100. **FIX 4 — slow-mo goal-lock camera snaps fast:** new tunable `SLOWMO_GOAL_SNAP_EASE=0.92` used for the goal-lock + blast framing (vs the unchanged gentle follow-ball `SLOWMO_FOLLOW_EASE=0.35`) so a short-range shot's camera fixes on the goal quickly to see the ball cross. **FIX 5 — stray match-start text:** ROOT CAUSE = the **`.toast`** element — it was `font-size:min(13vw,90px)`, **no background/border/frame**, and used the **body font (`Trebuchet MS`), not the game's Oswald** — so an event-driven info toast that lands at kickoff (e.g. the online "🎮 Opponent is a guest — casual match" ack, or a lingering equip/daily/region toast) renders as big plain unstyled text over the pitch; "only some games" because it's event-timed near match start. **FIX:** `.toast` now uses the **game font (Oswald) + a framed pill** (dark gradient bg, green border, radius — matching `#matchBanner`) at a sane size (≤32px); the **GOAL juice is preserved** via `.toast.slam` (boxless 90px dramatic, GOAL-only) and `showToast` now strips `.slam` so info toasts always get the framed look (verified: info toast = Oswald/32px/green-bordered pill; GOAL slam = 90px/boxless). Build clean; 0 tokens; harness passes; 0 console errors.
✅ **b87 = five contained fixes: Practice→equipped(straight-in) · per-card flip leave-reset · carousel sort (pe→jak→CHAD→ALEX, OVR desc, display-only) · slow-mo goal-snap (`SLOWMO_GOAL_SNAP_EASE`) · toast restyled to game-style framed pill (GOAL slam preserved).** No gameplay/stat/grabCap/netcode change.
> **📋 FUTURE GLOBAL TEXT-STYLE PASS (b87 scan, report-only — NOT fixed):** other text that isn't fully on the game font/style: `#verBadge` (build badge), `#potTag` (pot pill), `#liveCountHud` (👁 count), and the `xBtn` (𝕏) all use a generic `font-family:sans-serif` instead of the game font (they DO have styled pill containers, so minor). No raw browser-default/serif text exists anywhere (full DOM scan = 0). The match-start offender (the toast) is fixed in b87.
Last verified (b86, MONOTONIC xp/wins/losses sync — fixes "level randomly resets to 1"): the bug was a **destructive two-way sync** (local ↔ Supabase) with `|| 0` and last-writer-wins, so a stale/empty `0` (new device / cleared cache / private window / different per-origin localStorage) overwrote real progress; level is derived from xp, so xp→0 = level 1. **NOT duplicate accounts** (username unique index live; 0 dupes confirmed). **FIX (sync-logic only, no account/schema/data changes):** a new **`pushProfile()`** is now the single client→cloud profile writer — it calls a **`merge_profile` Postgres RPC that raises xp/wins/losses via `GREATEST()`** (awaited + 1 retry, replacing the old fire-and-forget `.update()`s that let cloud lag). `ensureOnline` no longer upserts `{xp:account.xp||0,…}` (that pushed 0 over cloud) — it now upserts **only `{id,username}`** to ensure the row exists, then `pushProfile()` raises values. `signInOnline` no longer does `xp:(prof.xp)||0` (that wiped good local) — it takes **`Math.max(localPrev, cloud)`** for xp/wins/losses, then `pushProfile()` so both sides converge UP. `addXp`/`awardEndOfMatch`/`recordResult` all route through `pushProfile()`. **Coins are intentionally excluded** (they can legitimately drop via wagers; settle_match RPC stays the sole coins authority — sacred law). **Net: no sync path can ever lower xp/wins/losses; a fresh/empty context can't wipe real progress.** Verified in-browser (built index.html): bug scenario localXp 1200 vs cloud 0 → stays **1200** (preserved, not wiped); empty local vs cloud 1200 → raised to 1200; both-real → higher; `pushProfile` guards (no sbUser→false, no throw), calls `merge_profile` with correct args **and no coins**, and retries on transient error (2 calls); the destructive raw writes are gone from all 5 paths; 0 console errors. Build clean; 0 tokens; harness passes. ⚠️ **NEEDS (one-time, user): paste the `merge_profile` SQL into the Supabase SQL editor** (printed in chat) — until then the client safely no-ops the cloud sync (xp stays local, **never wiped**); after it's applied, cloud sync resumes monotonically.
✅ **b86 = monotonic profile sync (fix: level resets to 1).** New `pushProfile()` → `merge_profile` RPC (GREATEST, never lowers) is the only profile writer; `ensureOnline` upserts id+username only; `signInOnline` takes max(local,cloud); xp/win paths awaited+retried, no longer fire-and-forget. No account/schema/data/uniqueness changes; coins untouched (settle_match only). **⚠️ Requires the `merge_profile` SQL to be run in Supabase (one-time).**
Last verified (b85, DIVE/LUNGE + FOUL + YELLOW/RED cards): wired the **DIVING** stat (display-only since b74) into a new **lunge** mechanic — a short forward dash on top of normal movement, **distinct from** the kick (charge+power, untouched) and the steal contest (def-vs-drb, untouched). **INPUT: RIGHT-CLICK** on desktop + a **dedicated mobile `LUNGE` button** (blue, tucked above-left of KICK so it can't be mis-hit; KICK untouched). Distance scales with DIVING and **defenders (no ball) reach further** (`LUNGE_DEF_BONUS`). **FOUL:** at lunge contact, if the lunger collects more **player than ball** (closer to/overlapping the opponent body than the ball, by a tunable margin) it's a foul on the lunger; reaching the **ball** first is a clean, legal tackle (wins possession). **CARDS:** 1st foul = YELLOW, 2nd = RED; a juiced card-flash slams on screen (football-style) + a per-player 🟨/🟥 mark on the HUD card. **GENERAL team-removal rule (not 1v1-hardcoded):** a RED removes the player; a team keeps playing while it has ≥1 player and **forfeits only at 0 players** — so 1v1 forfeits because a 1-player team hits 0 (opponent wins), and 2v2 (1v2 keep playing → 0 forfeit → other team to 0 wins) drops in later with **no new foul code**. **FOUL RESET** reuses the existing **goal-kickoff `reset()`** (no new reset written) with possession to the **fouled** side, cards re-stamped across the rebuild. **Online = host-authoritative** (host detects fouls, issues cards, removes players, runs the reset; streamed via a `foulSeq`/`fcd`/`fws` + `pca/tca/po/to` snapshot, same pattern as `goalSeq`/`sm`; guest mirrors the flash + counts + win). **CPU can lunge + foul** (new `lunge` axis on the AI difficulty tiers, aims at the ball). Guest predicts its own lunge locally (deterministic mirror) so it feels instant; host stays authoritative, reconciliation keeps it aligned — no desync. Verified in-browser (built index.html): diving↑ = longer lunge (boomer div17 atk 68px/def 106px vs grandpa div9 51/80), defender ×1.55, stamina −0.16; clean ball-win = no card + ball won; player-first = yellow; 2nd = red → `teamAlive('p')` false → `endGame('t')` (opponent wins); reset puts the ball on the fouled side (x=382 left, vx−1.2) + persists cards; 0 console errors; build clean, 0 tokens, harness passes. **Tunables:** `LUNGE_DIST_BASE/PER_DIV/DEF_BONUS/FRICTION/DURATION/COOLDOWN/STAMINA/MIN_STAMINA`, `FOUL_CONTACT_DIST/BALL_REACH/BALL_MARGIN`, `LUNGE_AI_RANGE`. **2v2 multi-player foul scenarios (1v2 keep-playing, double-red, 0-player forfeit on a 2-player team) become testable only when 2v2 ships — the rule is built general, not deferred.**
✅ **b85 = dive/lunge mechanic + foul detection + yellow/red cards + general team-removal rule.** DIVING wired → right-click / mobile-button lunge (defenders reach further, diving-scaled, cooldown + stamina). Lunge-into-player = foul (tunable player-vs-ball threshold); yellow→red; red removes the player; team forfeits at 0 players (1v1: opponent wins). Foul reset reuses the goal kickoff, ball to the fouled side. Host-authoritative + guest-mirrored (snapshot `foulSeq`/cards/out) + CPU lunges. Kick/grabCap/steal contest all untouched.
Last verified (b84, guest interpolation smoothness fix): **ROOT CAUSE** — the guest buffers snapshots stamped by RECEIVE time (jittery) and renders at `now − delay`, but the adaptive-delay FLOORS were below the buffer needed: host sends 60Hz (16.6ms/snap), yet `BALL_DELAY` floor was **20ms (~1.2 snaps, ~zero cushion)** and `INTERP_DELAY` floor **44ms (~2.6 snaps)**. So normal receive jitter constantly pushed the render point past the newest snapshot → `sampleAt` hit the EXTRAPOLATION branch (k=1+f) → the guest guessed velocity then SNAPPED back when the late packet landed → choppy/"guessing" **even on a good connection**. **FIX (all guest-side, named tunables):** raised floors `INTERP_FLOOR=80`/`BALL_FLOOR=48` (≈4-5 / ~3 snapshots of cushion) so jitter never underruns; adaptive `clamp(FLOOR + rtt*k, FLOOR, CEIL)` still grows under bad links (INTERP_CEIL=170, BALL_CEIL=85); `smoothRender()` eases only DISCONTINUITIES (per-frame jump > `RENDER_SNAP_PX=34` → ease `RENDER_EASE=0.35`/frame; smaller → track exactly, no added lag) on the opponent + ball → kills snap-back/rubber-band; underrun continues last velocity for ≤`EXTRAP_CAP_MS=100` then holds; host send loop subtracts `SNAP_INTERVAL_S` instead of resetting → steadier 60Hz. **Own-player prediction unchanged** (still reconciles to the freshest snapshot, so it stays real-time — only the OPPONENT renders 80ms back). **Host unaffected, authority unchanged, no desync** (guest changes are visual interp only). Verified in-browser: defaults=floors, adaptive grows (rtt250→155), smoothRender tracks-small/eases-big, a steady-buffer `applyNetState` interpolates (k=0.16, not extrapolating), no throw. Build clean; 0 tokens; harness passes. Trade: ~36ms more opponent latency for a lot more smoothness (per the task).
✅ **b84 = guest interpolation smoothness (root cause: interp-buffer floors too low → underrun → extrapolate+snap).** Raised the adaptive buffer floors, ease snaps via `smoothRender`, steadier send. Guest-side only; host/authority untouched. Tunables: `INTERP_FLOOR/CEIL`, `BALL_FLOOR/CEIL`, `RENDER_SNAP_PX`, `RENDER_EASE`, `EXTRAP_CAP_MS`.
Last verified (b83, P2P_DEBUG off for production): flipped `let P2P_DEBUG=true → false` (TURN confirmed working b78-b82). All 12 `[p2p]` console lines + the give-up verdict toast ("No direct P2P — relay mode (NAT needs TURN/STUN blocked)") are gated by `P2P_DEBUG`, so they're now silent for real players. Verified in-browser: `P2P_DEBUG===false`, a full `p2pStart` handshake emits **0 `[p2p]` console lines**, and the netcode is untouched (`loadTurn`/`TURN_SERVERS`/`p2pStart`/`p2pHandle`/`relay` all intact). The pre-existing connection-status toasts stay (⚡ only on a successful DIRECT link; "Relay mode" only if a live direct link drops — neither fires for a normal relay-only match). No netcode / TURN-wiring / relay-fallback change. Build clean; 0 tokens; harness passes. Re-enable diagnostics anytime via `P2P_DEBUG=true` (source or console).
✅ **b83 = P2P_DEBUG off (production).** One-flag flip silences all WebRTC handshake console diagnostics + the give-up debug toast; netcode/TURN/relay unchanged.
Last verified (b82, FINAL P2P policy — prefer-direct, use the region relay for relay-only): the user's b81 match showed `relay/srflx` via Cloudflare TURN at **450ms — WORSE than their Fly Sydney relay (~300)**, because Cloudflare's TURN anycast allocated a FAR edge. So b81's "keep the Cloudflare relay" was wrong for them. **Final policy:** keep P2P ONLY for a truly DIRECT pair (host/srflx ↔ host/srflx, ICE's auto pick when both are cone-NAT); for any RELAYED pair, decline and stay on the user's region-selected Fly WS relay (predictable, ~300 < Cloudflare's 450). Kept b81's load-TURN-before-gather, b80 UDP-only + honest ping, b79 disconnect-grace. **ROOT CAUSE of the user's high ping is NOT the relay choice — it's their connection latency to ANY relay**: the Fly box measures ~47ms RTT from a healthy connection (b68) but ~300 for them → ~75ms+ per leg, i.e. bufferbloat / a slow last-mile on ≥1 end. Sub-80 needs a DIRECT connection (their mate is symmetric-NAT → must get a cone NAT via UPnP/off-CGNAT/hotspot) AND healthy low-latency connections. Build clean; 0 tokens; harness passes. (No more relay-policy flip-flops — this is the stable default; the conversation is now network diagnosis.)
✅ **b82 = final P2P policy: prefer-direct, region relay for relay-only.** Cloudflare TURN anycast landed a far edge (450 > Fly 300) for the user, so relay-P2P is declined in favour of the region WS relay; true-direct host/srflx is still kept (the only real low-ping path). The remaining ~300 floor is the users' connection latency (bufferbloat/last-mile), not the game.
Last verified (b81, P2P — keep the Cloudflare-relay path + load TURN before gathering): the user's match log was decisive — selected pair `srflx/relay` (their direct address → mate's Cloudflare TURN allocation; the mate is symmetric-NAT), and `relay:0` locally because TURN loaded AFTER ICE gathering completed (race). Two b79/b80 mistakes corrected: (1) **don't decline relayed paths** — a Cloudflare-TURN (UDP) path, incl. `srflx/relay`, routes through Cloudflare's fast anycast edge and beats the single shared Fly WS relay; b79's "stay on the WS relay" was sending relay-only peers BACK to the slow ~300 box. Now `dc.onopen` always keeps P2P (getStats only LOGS direct vs relay). (2) **load TURN before gathering** — `p2pStart` is now `async` and `await loadTurn()` before constructing the `RTCPeerConnection` (and `p2pHandle` is async + awaits `p2pStart` on the offer-first path), so a symmetric peer actually gets its relay candidate instead of `relay:0`. Verified the async initiator path in-browser: offer + candidates signalled, DC created, `signalingState=have-local-offer`, no throw. UDP-only TURN (b80) + disconnect-grace (b79) + honest ping (b80) intact. Build clean; 0 tokens; harness passes. **Expectation: the user should now KEEP the Cloudflare `srflx/relay` path (≪ the Fly relay) — likely well under the old 300; whether it hits <80 depends on Cloudflare's AU edge proximity. Direct host/srflx (both cone-NAT) is still picked by ICE priority when available = lowest ping.**
✅ **b81 = keep Cloudflare-relay P2P + load TURN before ICE gathering.** Reversed b79's relay-decline (Cloudflare UDP relay beats the Fly WS relay); made `p2pStart`/`p2pHandle` async so TURN creds are present before candidate gathering (fixes `relay:0` race). The user's `srflx/relay` path is now used instead of falling back to the slow Fly box.
Last verified (b80, push toward <80ms — UDP-only TURN + honest ping display): two levers to get the user's SE-QLD pair to direct-connection latency. (1) **UDP-only TURN** — `loadTurn` now filters the Cloudflare iceServers to keep stun + `turn:…?transport=udp`, dropping `tcp`/`turns:` (TLS); verified against the real CF response it kept 2 UDP turn URLs + creds, dropped all TCP/TLS → relay candidates ~12→~4, so a DIRECT (srflx) pair isn't drowned out in ICE nomination AND any relay fallback is low-latency UDP (the TCP/TLS relay was what made it 500ms). (2) **Honest displayed ping** — the pill was showing the asymmetric *jitter-buffer* EMA (biased high by design); now `NET.rttDisp` (light symmetric smoothing) drives the shown number while the conservative `NET.rtt` still drives INTERP/BALL delay. b79 prefer-direct + disconnect-grace still in. Build clean; 0 tokens; harness passes. **Truth: <80ms needs a DIRECT connection (both peers cone-NAT). b80 maximises that chance + makes relay faster + the number honest; if ≥1 peer is symmetric-NAT/CGNAT, direct is physically impossible — the `[p2p] selected pair srflx/srflx vs relay/…` console line is the definitive test.**
✅ **b80 = UDP-only TURN + honest ping display (toward <80ms).** Fewer/faster relay candidates so direct srflx pairs win (and relay isn't the slow TCP/TLS path); shown ping = real RTT, not the conservative buffer EMA. The `[p2p] selected pair` log tells us if direct is achievable for a given matchup.
Last verified (b79, P2P stability — prefer-direct + transient-disconnect grace): live `/api/turn` confirmed returning Cloudflare creds (env vars set). Real-match logs from two SE-QLD desktops showed P2P **connecting then flapping** (connected→`disconnected`→reconnect) on a **TURN-relay** pair (candidates `host:2 srflx:2 relay:12`), giving 500ms+/unplayable — i.e. ≥1 peer is symmetric-NAT/CGNAT so ICE can only relay, and that relayed path is worse than the Sydney WS relay. **Two fixes (b79):** (1) **prefer-direct** — on DC open, `getStats()` the selected ICE pair; if it's a **relay** path, `p2pDown()` and stay on the (closer, more stable) WS relay; only keep ⚡ for genuine **direct** (host/srflx) pairs — so cone-NAT matchups get the low-ping win, relay-only matchups stay playable (~300) instead of the broken 500. (2) **transient-disconnect grace** — `connectionState='disconnected'` is recoverable, so ride it out 6s (only tear down if it doesn't recover, or on `failed`) — kills the P2P/relay thrash. Verified the pair-type detection on a loopback (host↔host → `relayed:false` → keeps P2P). TURN scaffold/loader (b78) intact. Build clean; 0 tokens; harness passes. **Honest limit: two symmetric-NAT peers can't get a direct path — relay (~300) is their floor; the ⚡ ~50-70ms win needs ≥1 cone-NAT peer.**
✅ **b79 = P2P stability fix (prefer-direct + disconnect grace).** Decline TURN-relayed P2P (stay on the better WS relay), keep ⚡ only for direct host/srflx pairs; treat `disconnected` as transient (6s grace, no thrash). Fixes the 500ms/unplayable flapping the user hit; cone-NAT matchups still get the direct low-ping path.
Last verified (b78, Cloudflare TURN — serverless integration): the TURN fix from the b77 diagnosis is now wired the proper way. New Vercel function **`api/turn.js`** mints SHORT-LIVED Cloudflare TURN creds server-side (`POST …/v1/turn/keys/{id}/credentials/generate-ice-servers`, ttl 24h) using env-var secrets — the key token never reaches the browser. Client: `loadTurn()` fetches `/api/turn` (called at the top of `NET.connect`, parallel with the WS connect so creds are ready before `p2pStart`), populates `let TURN_SERVERS` which is spread into the `RTCPeerConnection` iceServers; cached ~1h. Verified: graceful fallback (no `/api/turn` → no throw, `TURN_SERVERS` stays `[]` → STUN-only, nothing breaks), iceServers still constructs, a real CF response maps to `[{urls,username,credential}]`. Both syntaxes pass; build clean; 0 tokens. **⚠️ NEEDS (one-time, user): create a Cloudflare Realtime/Calls TURN key → set `TURN_KEY_ID` + `TURN_KEY_API_TOKEN` in Vercel env vars → redeploy. Then two CGNAT peers get ⚡ + ~50-70ms.** `P2P_DEBUG` stays on to confirm; set false after.
✅ **b78 = Cloudflare TURN via serverless `/api/turn` (the b77 fix, done right).** `api/turn.js` generates short-lived ICE creds from a CF TURN key (secret in Vercel env); client `loadTurn()` fetches + merges them into the P2P iceServers (STUN-only fallback if unset). Once the env vars are set, symmetric-NAT/CGNAT peers establish P2P. No change to relay fallback / snapshot sync / matchmaking.
Last verified (b77, P2P latency diagnosis + TURN scaffold): **ROOT CAUSE = genuine NAT traversal, NOT a code bug.** Traced the whole WebRTC handshake — offer/answer/ICE-candidate trickle over the WS relay (`signal`→`send({t:'relay',{rtc}})`→server forwards BOTH ways→`onRelayData` handles `d.rtc` FIRST→`p2pHandle`), candidate queueing/`drainCand`, the relay server's `m.t==='relay'` forwarder — **all correct, no regression** (sm/cm/cs/goalSeq/charStats/region builds never touched the rtc path). Proved it via an in-page **loopback test**: the exact offer/answer/candidate pattern opens the DataChannel (ICE `connected`) when candidates can route. So STUN-only + two real NATs is the issue: **STUN can't traverse SYMMETRIC NAT / CGNAT** (very common on AU residential NBN + all 4G/5G home internet) → ICE finds no working pair → both peers fall back to relaying every packet through the Sydney Fly box → the steady 300-400ms, no ⚡. **The fix is a TURN server.** Added: (1) **diagnostic logging** (`P2P_DEBUG=true`) — console logs ICE state, local+remote candidate TYPES (host/srflx/relay), DC-open, and a give-up verdict ("srflx gathered but no pair → symmetric NAT/CGNAT → needs TURN" vs "no srflx → STUN blocked"), plus a toast; (2) **`TURN_SERVERS` scaffold** (empty = STUN-only, unchanged) spread into `iceServers` — paste Cloudflare TURN creds to enable. Relay fallback + snapshot sync + matchmaking all unchanged. Build clean; 0 tokens; harness passes. **⚠️ NEXT: supply Cloudflare TURN creds → paste into `TURN_SERVERS` → ⚡ + ~50-70ms.**
✅ **b77 = P2P-no-connect diagnosis: STUN can't traverse CGNAT → needs TURN (scaffold added).** Not a bug — handshake/signalling proven correct (loopback DC opens; relay forwards both ways; no regression). STUN-only fails for symmetric-NAT/CGNAT peers (AU home internet). Wired `TURN_SERVERS` (empty, ready for Cloudflare creds) + `P2P_DEBUG` logging to confirm live.
Last verified (b76, Oldjak elite-defender fix): `oldwoj` (displays OLDJAK) was a stale OVR 77 (Stone) — corrected to the intended ELITE DEFENDER: PAC 8 · DEF 20 · STA 19 · DRI 11 · POW 18 · DIV 18 → **OVR 94**, grab 52 (unchanged). He now renders **Netherite** purely via the overall-driven `oreTier` (NO manual override added — `o>=94` band, function untouched). Verified: OVR 94, tier netherite, **Tackle 5★ + Grab 5★ intact**, in-game grabCap still 52, gameplay reads def 20 / drb 11 (pace 8 → still a slow wall). Netherite tier is now ALEX(100, attacker) + OLDJAK(94, defender) — the two flagships. All 13 other cards UNCHANGED (overalls/tiers identical to b75; confirmed against the doc table — only oldwoj diverged). Archetype label updated "Defensive Wall" → **"Defensive Flagship"** (mirror of ALEX's "Attacking Flagship"; grandpa/EPPE stays the budget "Defensive Wall"). Build clean; 0 tokens; harness passes.
✅ **b76 = Oldjak (oldwoj) elite-defender correction.** One `STATS` row: pace8/def20/sta19/drb11/pow18/div18 (was 8/19/16/9/16/9) → OVR 77→**94**, grab 52 unchanged → auto-Netherite. Single source of truth → locker card (OVR/stats/stars/tier) + gameplay (def/drb/power/grabCap) all update from the one edit. No tier override; tier stays overall-driven. (Note: the design-doc table still lists oldwoj at 77 — stale; this build is the approved correction.)
Last verified (b75, locker visuals — display-only, the two parts b74 missed): **(A) half-star ratings** on the extended card — Grab/Hold/Tackle now render as ★ out of 5 (half allowed) mapped from the real values (grab 48→1★ .. 52→5★; Hold from Dribbling/4; Tackle from Defending/4) — verified Eppe/Oldjak grab+tackle 5★, attackers grab 1★, Pdidpe/Pepe hold 5★; on/off-ball stay numbers. **(B) ore tiers** by OVERALL band, cosmetic only (no power): Stone 75-79, Iron 80-83, Gold 84-87, Diamond 88-93, Netherite 94-100 — verified ALEX=Netherite (dark charcoal+purple, animated shimmer), Diamond CHAD/MUSKPE/PEPE, … Eppe=Stone; escalation obvious. Animations are transform/opacity only, **active card only** → mobile-FPS-safe. **Stats / overalls / grabCap / gameplay UNCHANGED from b74** (verified grabCap 48.5/52, OVR 100/75 identical). 0 tokens/placeholders; harness passes. ⚠️ The spec example "Oldjak Netherite" looks like a typo — by overall (77) Oldjak is **Stone**; implemented strictly by-overall per the rule.
✅ **b75 = locker visuals: half-star ratings + ore tiers (display-only).** PART A: `grabStars`/`holdStars`/`tackleStars` (nearest-half) + `starHTML` render full/half/empty ★ out of 5; the extended card's Grab radius / Hold-ball / Tackle win rows now show stars instead of raw `48.5`/`84%`/`27%` (gameplay still reads the real values). PART B: `oreTier(c)` from `overallOf` → Stone/Iron/Gold/Diamond/Netherite; the carousel slot gets a `tier-X` class driving frame border + material gradient + colored glow (+ the top accent stripe), with a compositor-friendly sheen layer (`.loSheen`) — shine-sweep (transform) for Gold/Diamond, purple opacity shimmer for Netherite — animated ONLY on the active card. Netherite is the special dark-charcoal+purple card (light text overrides). Equipped ring changed to an `outline` so it stacks with the tier glow. Reuses the `owned`/cosmetic-rarity seam. NO stat/overall/grabCap/gameplay change.
Last verified (b74, GRADED card model — replaces equal-totals): all 14 rescaled to 6 FRONT stats /20 (Pace·Def·Sta·Drb·Pow·Div) → OVERALL /120 (graded ladder 75–100); verified every card's OVR + grab matches the approved table, pace-split sums to pace, `grabCap` reads per-card grab (range 48–52), renames (doomer→**RUGGEDJAK**, oldwoj→**OLDJAK**) display while sprites/STATS still resolve, Diving displays (DIV) but is unwired, the card front shows OVR top-left + 6 stats, the extended back shows on/off split + grab radius + Hold%/Tackle% + record, the displayed Hold/Tackle % **equals the in-game contest math** (grandpa tackle 51% == in-game p 51%), gameplay smoke test runs (movement/steal/grabCap, no errors), "‹" back button in place. Single source of truth = the one `STATS` table. **grabCap now VARIES per card (48–52) — the one place the hitbox varies, tight + visible.** 0 tokens/placeholders; harness passes. **Diving mechanic + matchmaking-by-overall are later builds.**
✅ **b74 = card grading / stat rework (graded model).** Replaced the equal-totals 6-stat model with the approved graded table: 6 front stats /20 → OVERALL /120 (unequal overalls, a power ladder). `STATS` rows now `{name,abbr,arch,pace,def,sta,drb,pow,div,grab}`; `overallOf`=sum6, `speedSplit` derives on/off-ball summing to pace, `statDerived` rescaled for /20 (KICK_BASE/SLOPE, STAM_*, STEAL_WEIGHT 0.06→0.035, DEF_CLOSE_BONUS 0.06→0.03, glue /20). `grabCap(pl)` returns the card's `grab` (48–52, stored on the player) instead of flat 48. Locker card front = OVR + PAC/DEF/STA/DRI/POW/DIV; back = on/off split + grab radius + Hold% + Tackle% (derived from the SAME `stealChance` the contest uses, vs a CONTEST_AVG=14 baseline) + the b72 record. archetype moved into STATS (CHAR_META removed). Renames are display-only (keys/sprites/lookups unchanged). Diving display-only. Back/exit button → smooth top-left "‹". **Fairness note: equal-totals is intentionally GONE — cards are graded; grabCap varies tightly + visibly. Kick power still charge+power only.**
Last verified (b73, menu-over-match bugfix): starting a **Quick Match** or **Wager** match left the **PLAY-hub overlay** (`modeSelectScreen`) / **wager screen** (`wagerSelectScreen`) painted over the live game — root cause: the match-start hide-lists were hardcoded and omitted those hub overlays; the search screen (`matchScreen`) hid them only until the match began, then they re-surfaced (`.overlay` z-20, near-opaque, above the canvas — and on mobile it also blocked the touch controls). Fix: match-start now calls `hideAllOverlays()` (dismisses EVERY `.overlay`) in `_beginGame` + `_beginGameOnline` + `startQuickMatch`, instead of a list that could miss screens. Verified at 375px: quick/wager/practice all start with **0 menu overlays** over the match, HUD + player cards present, pause still opens/closes; in-match chrome (`#hud`/`#touch`/cards aren't `.overlay`, untouched). Practice never had the bug (it already routed through `hideAllOverlays`). grabCap 48; 0 tokens/placeholders; harness passes. NB: reproduces on desktop too; most impactful on mobile (mobile-first + overlay blocks the joystick).
✅ **b73 = fix: menu overlay left on top of the live match (quick/wager).** `modeSelectScreen`/`wagerSelectScreen` weren't in the match-start hide-list → visible over gameplay. Now `_beginGame`, `_beginGameOnline`, and `startQuickMatch` use `hideAllOverlays()`. No regression to pause / in-match chrome / desktop.
Last verified (b72, Locker BUILD 2 — per-character stat tracking): hooks observe existing events (steal contest → dribble/tackle; `scoreGoal` → gf/ga/own-goal; `endGame` → W/L) and record per character. Verified in-browser: dribble 67% (2/3), tackle 50% (1/2), gf2/ga2/og1/W1 attribute correctly; accumulate across matches (W1→2); **online host commits only its own char + relays the guest's (no double-count)**, guest applies the relayed deltas to its own profile; card BACK now shows real numbers (not 0/"—"). **Fixed a b70 bug**: `charRecordAll()` double-parsed `DB.get` → always returned `{}` (so the back could never show data). ⚠️ **REQUIRES SQL** for cross-device: `alter table profiles add column if not exists char_stats jsonb;` (local works without it). grabCap 48 (observe-only, no gameplay/steal/scoring change); 0 tokens/placeholders; harness passes. ⚠️ `LOCKER_STAT_DEFINITIONS.md` was not on disk — built to the task-body event definitions.
✅ **b72 = Locker BUILD 2: per-character, per-player match stat tracking.** Observe-only hooks (no gameplay change): the existing **steal contest** (`updatePossession`) feeds one pressure event into BOTH the carrier's dribble (success = kept possession) and the defender's tackle (success = won the ball) — numerator + denominator both tracked; **`scoreGoal`** → scoreboard-consistent goals-for/against + an own-goal sub-flag (conceding side = `ball.shooter`); **`endGame`** → W/L. A per-match accumulator `matchStat{p,t}` runs ONLY on the authoritative side (host + offline — the guest never runs the sim, so no double-count) and commits at match end via `commitCharStats` (local `sh_crec_<user>` + guarded Supabase `char_stats` jsonb mirror). **Online attribution:** host commits its own (`players.p`) and **relays the guest its own deltas** (`players.t`) via `{charStats}`; the guest applies them in `onRelayData` → each player's stats land on their own profile exactly once. Card BACK (b70 UI) reads the store unchanged. **Scope decision (stated):** tracks every real match WITH an opponent — online + practice-vs-CPU + local 2P; **solo (no opponent) excluded**. To make it online-ranked-only later, change `statTracking=oppActive` → `oppActive && ranked`. Fairness: observe-only — grabCap constant 48, steal/scoring/gameplay untouched.
✅ **b71 = locker button restyle (UI only).** EQUIP label "EQUIP <NAME>"→"EQUIP"; CTA off-green → navy ink (`linear-gradient(#27517a,--fc-ink)`); `.loBackBtn` + `.loArrow` given the matching FIFA-clean secondary treatment. EQUIPPED-state styling unchanged.
✅ **b71 = locker button restyle (UI only).** EQUIP label "EQUIP <NAME>"→"EQUIP"; CTA off-green → navy ink (`linear-gradient(#27517a,--fc-ink)`); `.loBackBtn` + `.loArrow` given the matching FIFA-clean secondary treatment (white surface, `--fc-line` border, ink text, accent-on-hover). EQUIPPED-state styling unchanged. No locker logic/persistence/carousel/card-data change.
Last verified: **Locker BUILD 1 of 2** — holographic 14-card carousel (active centred, neighbours peek; verified active `scale(1)`, neighbour `scale(.82)` op .6), flip front↔back (front = PACE/archetype/6 stats from STATS; back = per-char record with 0/"—"), EQUIP sets+persists (local `sh_equip_<user>`; verified persists across a simulated reload; equipped ring+badge; menu button shows equipped name), all flows use the equipped char (quick/wager bypass the picker straight to matchmaking; practice/host hide the carousel + show "Playing as <name>"; guest sends its equipped). FIFA-clean light look verified on desktop + 375px mobile. grabCap 48; 0 tokens/placeholders; harness passes. ⚠️ **OPTIONAL SQL** for cross-device equip: `alter table profiles add column if not exists equipped text;` (guarded — local works without it). ✅ **Verified against `BASED_CUP_LOCKER_DESIGN.md`** (the user added it after the build) — the build conforms: the doc's 3 open calls (grid-vs-carousel, button location, extra card content) were all decided by the build prompt and built as specified (carousel, top-level button, card-back record). **BUILD 2 (next) = per-character match stat-tracking** to populate the card back.
✅ **b70 = Locker BUILD 1: holographic carousel + equip flow + card flip (FIFA-clean tokens).** New `#lockerScreen` overlay: a floating holo carousel of all 14 (active centred+large, adjacent edges peek for depth), swipe/drag (pointer events) + arrows + dots, gentle float anim. Each card flips (tap) — FRONT: head art, name, archetype (`CHAR_META`), 6 stat bars + **PACE** headline, all read from `STATS` (single source, no duplicated numbers); BACK: per-char record (W/L, GF, GA, own goals, dribble%, tackle%) from a `getCharRecord()` store that returns 0/"—" until BUILD 2 fills it. **EQUIP** sets the active char as `equippedChar` (+ `humanChar`), persisted local (`sh_equip_<user>`) and mirrored to Supabase `profiles.equipped` (guarded try/catch — needs the column; never breaks if absent); equipped card shows a ring + ✓ badge; defaults pepe. **Replaced the per-match picker entirely**: quick/wager set equipped + queue straight in; practice/host keep their mode/opponent options but the character carousel is hidden (shows "Playing as <name>"); online guest plays its own equipped char (`setupGuestWager`). Menu **🎽 LOCKER** button near PLAY shows the equipped name. **Owned seam**: `isOwned(c)` (all true) gates the carousel + equip + shows a lock layer for future rarity — architected, not built. Fairness untouched: display+selection only, `grabCap` constant 48, no gameplay/stat change. **Design tokens**: FIFA-clean light palette (`--fc-*` on `#lockerScreen`) = the new design system for the coming UI remodel (scoped here so the dark UI isn't regressed).
✅ **b69 = keep-away aim arrow removed + leaderboard → Hourly + All-Time.** PART 1: the two `drawAim` calls are gated `if(gameMode!=='keepaway')` — possession mode shows no shot-aim arrow; goals mode unchanged; steal contest (defense-vs-dribble) untouched (it's in the movers, not the draw). PART 2 (diagnosis first): the old "12h" board was a **rolling** window (`created_at >= now()-interval 12h`), NOT a reset — data in Supabase `match_results` via Vercel `/api`. Rebuilt into two tabs in the board screen: **Hourly** = current Brisbane/AEST clock hour (`where created_at >= date_trunc('hour', now() at tz Brisbane) at tz Brisbane`) → hard-resets at each :00 because the boundary moves (NO deletes); **All Time** = same table, no time filter. **Hourly reset can NEVER touch All-Time** — both are read-only SELECTs over the same immutable table; all-time has no time predicate, hourly's "reset" is just the moving `where`. Client `LB.boardMode/rankMode('hourly'|'alltime')`, `api/leaderboard.js` routes `?mode=` to the right RPC (legacy rolling kept as fallback), home-menu top-3 + label now "this hour". **SQL the user must paste in Supabase (4 funcs, zero comments) — not auto-applied:** `hourly_leaderboard`, `hourly_player_rank`, `alltime_leaderboard`, `alltime_player_rank` (in `supabase-schema.sql` + the chat paste block).
✅ **b68 = US region disabled (dead relay) + STUN redundancy.** Server check found: Sydney + Singapore relays healthy (1 machine each — queue not split; WS RTT 47ms / 142ms), but the `basedcup-lobby-us` app **does not exist** while the UI still offered a 🇺🇸 US button → anyone picking US got a silent dead-connection. Fix: `REGION_SOON={us:true}` gates selection (segment + popup greyed "soon", `setRegion` toasts + no-ops, `curRegion` sanitizes, `NET.connect` rewrites the dead US URL, one-time migration of saved US state). Also added 2 more STUN servers (still **no TURN** — symmetric/CGNAT users keep relaying over WS; add TURN when a provider is chosen). Re-enable US by removing it from `REGION_SOON` once the US relay is live.
✅ **b67 = intro door overlay aligns to the real painted door (any aspect).** The b65/b66 door used fixed viewport-% which drifts off the image under `object-fit:cover` (the image is scaled+cropped per aspect). Fix: `layoutIntroDoors()` computes the cover scale/offset for the 1600×900 image and places `#introDoors` (and the dive transform-origin) at the door's IMAGE-fraction location in viewport px — recomputed on resize. Door fractions measured from the user's red-outlined door image. Now lines up on desktop + mobile + any aspect.
✅ **b66 = slow-mo Pass 2: out-of-range gate.** Slow-mo now ALSO requires the shot to be player-unstoppable. `shotIsPlayerUnstoppable(side)` (run once as the shot enters the zone): project the ball to the goal line (time-to-cross from pos+vel), then for the DEFENDER (opponent of the scorer — L goal defended by `players.p`, R by `players.t`) sample the path and check if they can reach ANY point within `grabCap`(48) before the ball, using their real max speed (`maxOff` + `DEF_CLOSE_BONUS`·defense). Reach → STOPPABLE → no slow-mo; can't reach → arm. Posts/walls: only suppress a CLEAR wide miss (path exits the mouth by > `SLOWMO_POST_GRACE`); a near-post graze still arms (intended drama). `FORCE_GOAL_SLOWMO` bypasses this too. Host-authoritative (runs where `maybeArmSlowmo` does) → guest follows via the existing `sm` flag. **Camera / explosion / decider gating (2nd-half/last-20s/margin≤1) / netcode / score / grabCap all unchanged** — this only ADDS a condition.
✅ **b65 = intro doors swing (fix).** b64's "doorway opening" just grew a black hole. Now the doors SWING: leaves are **invisible when closed** (you see the actual painted door — no fragile colour/width match) and on `.opening` a dark interior + dark **rectangular** door leaves swing out (hinged on the outer edges), on the door body below the black lintel (`--dx:50% --dy:72% --dw:17% --dh:30%`, tunable). Then the dive zooms into the dark opening → menu.
✅ **b64 = intro reveal: actual building + "enter it" rework.** Two fixes over b63: (1) baked the real image — saved the pasted blue/white/gold dome building (from `Downloads/ChatGPT Image Jun 30…png`) as `intro_reveal.jpg` (1600×900). (2) Reworked the animation so the BUILDING is the visible star: the b63 fake brown door-leaf panels covered it ("just a door") → removed them. Now: hood yanked off → building snaps clear+bright (you SEE it) → its **doorway** (`#introVoid`, an arched dark shape at `--dx/--dy/--dw/--dh`) **parts open from the door seam** to reveal the dark interior → fast **dive** (`#introReveal.dive` scale-9 toward the doorway) as `#introBlack` fills → menu. Doorway vars tuned to the image (`--dx:50% --dy:67% --dw:13% --dh:38%`), still nudgeable. Tap-skip + safety timer + once-per-session guard + no-image fallback kept. Front-end only.
✅ **b62 = slow-mo camera target fix.** b61's camera did a generic eased chase of `ball.x` → lagged the moving ball, framed "randomly", couldn't see the ball go in. Now at arm-time it picks framing from where the shot was STRUCK (`ball.kickX`, set in `tryKick`) vs the halfway line: struck from **own half / beyond halfway (long-range) → FOLLOW the ball** the whole way in; struck from **inside the opponent's half (short-range) → LOCK on the target goal** so you clearly see it cross. Decided once at arm (never flips mid-slow-mo); during the blast it frames the goal. Online-synced via streamed `cm`/`cs` (mode + side, same pattern as `sm`/`goalSeq`). Tighter follow ease. Slow-mo timing/gating + explosion + netcode + scoring all unchanged.
✅ **b61 = smooth slow-mo + cinematic zoom.** The b60 frame-pulldown (skip ~70% of frames) read as buffering/lag. Replaced it with **`motionScale`** — slow-mo scales per-frame motion (eased) so the ball moves a little EVERY frame at 60fps = buttery, not choppy. Applied in `applyMove`/`applyMoveAI`/`stepBall`/`predictGuestSelf`. Added a **cinematic camera zoom** (`camZoom`/`camFocus`, `SLOWMO_ZOOM=1.5`) that eases in toward the ball (or goal during the blast), clamped to stay on-field, and eases back out — "all eyes on the ball." Gating + sync (`sm` flag) + explosion unchanged from b60. Still visual-time-only: match clock/netcode/score untouched.
✅ **b60 = slow-mo anticipation + gating (Pass 1 of 2).** Slow-mo now ARMS as a fast goal-bound shot enters a zone in front of the mouth (so you watch the ball cross in slow-mo, THEN the explosion), and only on dramatic deciders: **2nd half + last 20s + pre-goal score margin ≤ 1** (or `FORCE_GOAL_SLOWMO`). Reading-flag: "FX clock only" interpreted as **visual time only** — slow-mo dilates the rendered sim via frame-pulldown + the FX clock, but **never** the match clock (`timeLeft` stays real-time), the netcode protocol, or the scoring/half OUTCOME. Online-synced via a streamed `sm` flag (same pattern as `goalSeq`): host pulls down its sim → guest follows + pulls down its own prediction. **The explosion is unchanged** — only WHEN slow-mo arms/fires changed. If the gate fails the explosion still plays, just at full speed. **Pass 2 = opponent-out-of-range detection (not done here).**
- Slow-mo tunables (near `goalLock`): `GOAL_SLOWMO_SCALE=0.30` (1.0=off), `GOAL_SLOWMO_MS=320`, `SLOWMO_ZONE=110`px, `SLOWMO_BALL_SPEED=6`px/f, `SLOWMO_LAST_SECS=20`, `SLOWMO_MARGIN=1`, `SLOWMO_ZOOM=1.5` (1.0=no zoom), `SLOWMO_ZOOM_EASE=0.14`, `SLOWMO_FOLLOW_EASE=0.35`, **`SLOWMO_DEF_SPEED_K=1.0`** (defender speed mult in reach calc; <1=arms more), **`SLOWMO_REACH_MARGIN=14`px** (defender must beat the ball by this to "stop"; higher=arms more), **`SLOWMO_POST_GRACE=34`px** (path may exit the mouth by this and still arm), `SLOWMO_DEBUG=false` (console.log the reach calc), `FORCE_GOAL_SLOWMO=false`.
- **FORCE toggle** (feel-test every goal): edit `let FORCE_GOAL_SLOWMO=false` → `true` in `game_template.html` + rebuild, OR live in the browser console: `FORCE_GOAL_SLOWMO=true`.
✅ **b59 = goal juice (purely visual) + slow-mo + loading cleanup.** On a goal both heads + the ball detonate AWAY from the scored-in goal (Rocket-League style, scaled by proximity), with a goal flash/shockwave, screen shake, GOAL slam-in text, and a synth boom. Online-synced via a `goalSeq` counter in the snapshot (same proven pattern as the rebound event) — host increments + detonates, guest detonates on the counter change; **authoritative reset/score/half logic unchanged** (FX is render-only, never moves the real objects). Brief slow-mo beat on the goal, strength = one constant `GOAL_SLOWMO_SCALE` (set 1.0 to disable). Loading: removed the generic `> based.cup booting…` boot line from the one-time terminal intro; kept `> escorting goyim into the system…` + the static loader text. No gameplay/stat/grabCap change.
- Goal-juice tunables (near the `goalLock` global): `GOAL_SLOWMO_SCALE=0.30` (1.0=off), `GOAL_SLOWMO_MS=320`, `GOAL_FX_MS=950`, `GOAL_SHAKE=24`.
✅ **b58 = re-normalise on FACE WIDTH (not bbox height).** b57 made every head the same total HEIGHT, but the eye reads FACE size, so heavy-hair chars got tiny faces and bald/big-face chars looked oversized (cheek width varied 31% under height-norm). Switched `process_sprites.py` scale basis from `TARGET_HEAD_H` back to **`FACE_W=240` (cheek width)** — the perceived-size metric — with `ADJUST={boomer:0.85, okeyjak:1.20, pdidpe:0.88}` for bald-cranium/3-4 misreads, a top-fit safety (no hair clip), bbox-centre + side-fit (no side clip). **Kept b57's `CHIN_OVERRIDE={'okeyjak':0.88}` chin fix.** Hair/ears/hats now extend naturally above (silhouette height varies = correct). Re-cut all 14, rebuilt. grabCap/stats/gameplay untouched.
✅ **b56 = pace-spread tuning.** b55's on/off split was too subtle (~6% felt). Widened the stat→speed mapping only (`SPEED_FLOOR`/`SPEED_SLOPE`), no logic/stat/grabCap change. Speeds now: elon on 6.10 / off 7.10 (gap 0.32→1.00), pepe on 6.60 / off 6.10 (gap 0.16→0.50), EPPE/oldwoj on 3.60 / off 4.10. Fastest-off (elon 7.10) vs slowest-on (EPPE 3.60): range 1.12→3.50, ratio 1.24×→1.97×.
✅ **b55 = Phase 2 stat model** (per BASED_CUP_STAT_DESIGN.md). Old 3-stat (speed/power/stamina ~21) → 6 assignable stats (on-ball, off-ball, power, defense, dribble, stamina), every char totals **36**, lopsided spread = archetypes. Pace (display) = on+off. **Phase 3 (character menu) NOT built yet** — the old 3-bar select carousel got a 1-line compat shim so it doesn't render NaN. b54 AI tier logic untouched (it just reads the new stats via the shared movers).
This folder is the git checkout (branch `main` tracking `origin/main`); deploy = `git push origin main`.

🔖 **Restore point: tag `ui-v1-classic` = commit `5ca3519` (b58)** — the classic UI/look before the goal-explosion/slow-mo visual work. Pushed to origin. Roll back with `git checkout ui-v1-classic` (or `git reset --hard ui-v1-classic`).

### Stat-model tunable constants (b55) — names + current values (near the top of the script)
`STEAL_BASE=0.30` (even-match steal chance/contest), `STEAL_WEIGHT=0.06` (±chance per defense−dribble point), `STEAL_MIN=0.05`/`STEAL_MAX=0.85` (clamp), `STEAL_TRY_GAP=22`f (between contest attempts), `STEAL_COOLDOWN=40`f (grace after a steal). `DRIBBLE_GLUE_MIN=0.30`/`DRIBBLE_GLUE_MAX=0.55` (glueBall tightness from dribble 1..10; old fixed 0.40). `DEF_CLOSE_BONUS=0.06` (off-ball speed per defense point when chasing the carrier). Speed (b56 tuning): `maxOn=SPEED_FLOOR+onball*SPEED_SLOPE`, `maxOff=SPEED_FLOOR+offball*SPEED_SLOPE` with **`SPEED_FLOOR=2.1`, `SPEED_SLOPE=0.50`** (was `4.2 + stat*0.16`); `kickPow=9+power*0.9` (unchanged).
**Sprint/stamina resolution (flagged decision):** sprint stays a universal, stamina-gated `×SPRINT_MULT` multiplier applied on top of WHICHEVER base is active (on-ball base while possessing, off-ball otherwise). The split only swaps which base sprint multiplies; the stamina economy is unchanged. Closing-speed bonus is added flat (outside the sprint multiply) so it stays modest.

---

## Status board (read this first)
| Feature / system | Status | Build |
|------------------|--------|-------|
| Core gameplay (move/charge-kick/possession/goals/walls) | ✅ done | — |
| Hitbox equality (`grabCap` identical for all) | ✅ done · sacred | — |
| Head-soccer render (no ring, face-centred, team pool) | ✅ done | b47 |
| Face-normalised sprites (chin-aligned, consistent size) | ✅ done | b46 |
| 14-character roster | ✅ done | b43→b50 |
| Online netcode (host-authoritative, relay) | ✅ done | b40 |
| Guest interpolation smoothness (adaptive buffer floors + snap-ease) | ✅ fixed | b84 |
| WebRTC P2P transport (STUN-only, ~halves RTT) | ✅ done | b39 |
| Accounts (local + Supabase), guest play, PIN auth | ✅ done | — |
| Monotonic profile sync (`merge_profile` GREATEST) — fixes level-reset-to-1 | ✅ done · ⚠️ needs SQL run | b86 |
| $BASED coins + wager mode (settle_match RPC) | ✅ done | — |
| Friends / chat / inbox (Supabase realtime) | ✅ done | — |
| Leaderboard — Hourly (AEST clock-hour, hard reset) + All-Time tabs | ✅ done · ⚠️ needs SQL run | b69 |
| Keep-away hides the aim arrow (possession mode, steal intact) | ✅ done | b69 |
| Daily tasks, XP/levels, achievements, profile + avatar | ✅ done | — |
| Server-region picker, live "playing now" counter | ✅ done | b38 |
| Phantom wallet connect, invite links | ✅ done | — |
| Keep-away mode + rebound bonus, half-time | ✅ done | — |
| Mode-select (Goals/Keep-Away) reachable for ALL flows | ✅ done · b101 restored via lobby MODE selector (was orphaned in #selectScreen since b70/b87) | b101 |
| Lobby bottom = TYPE + MODE selectors + one PLAY (no straight-launch chips) | ✅ done | b101 |
| Mobile-LANDSCAPE lobby fits (no scroll, PLAY reachable) — landscape media query | ✅ done · b102 (phones >640 wide were on the desktop layout → overflow) | b102 |
| Guest SOL prompt — ✕ close + session-persist (no re-nag) | ✅ done | b102 |
| Profile chip (`#acctBtn`) never covers menu back buttons / locker | ✅ done · b103 (`.overlay` z56 > chip z55; lobby pinned z20) | b103 |
| BACK buttons prominent + big tap target (FIFA-clean, all screens) | ✅ done | b103 |
| In-match ping pill at the bottom (not mid-screen) | ✅ done | b103 |
| Landscape lobby breathing room (less squishy, still fits/no scroll) | ✅ done | b103 |
| Guest own-player prediction feels responsive (no reconciliation drag) | ✅ done · b104 (`predictGuestSelf` reconcile `err>150?0.16:0.04`) | b104 |
| Main-menu card: narrow portrait — name+OVR+ore EMOJI + OFFENSE/DEFENSE ★/5 (back=record) | ✅ done | b105 |
| Six ore tiers (🪵🪨⚙️👑💎💀 by OVR band) — emoji on main-menu card, bands consistent in locker | ✅ done | b105 |
| Lobby live sprite shrunk (~half; room to roam) | ✅ done | b105 |
| Mobile controls + overlay scroll + rotate nudge | ✅ done | b41/b42 |
| Mobile movement fix (full-tilt joystick = full normal speed) + dedicated SPRINT button (no rim-sprint) | ✅ done | b95 |
| Match-start hides ALL menu overlays (no menu over live game) | ✅ fixed | b73 |
| One-time intro (terminal → curtain → reveal) | ✅ done | b44/b45 |
| On-screen error reporter (red bar + 📋 COPY) | ✅ done | b48/b49 |
| Input handlers robust to synthetic key events (autofill/extensions) | ✅ done | b53 |
| AI difficulty tiers (easy/normal/hard genuinely escalate) | ✅ done | b54 |
| 6-stat model (on/off speed, power, defense, dribble, stamina; total 36) | ✅ done | b55 |
| Pace spread widened (on/off + char-to-char clearly felt) | ✅ done · feel-test pending | b56 |
| Sprites normalised on FACE WIDTH (consistent perceived size); okeyjak uncropped | ✅ done | b58 |
| Goal explosion + flash + screen shake + GOAL slam (online-synced) | ✅ done | b59 |
| Goal slow-mo beat (tunable `GOAL_SLOWMO_SCALE`, 1.0=off) | ✅ done | b59 |
| Slow-mo anticipates the crossing + gated to last-20s deciders (+FORCE toggle) | ✅ done | b60 |
| Slow-mo smooth (per-frame motion, no buffering) + cinematic zoom to the ball | ✅ done | b61 |
| Slow-mo camera target: follow ball (long shot) / lock goal (short shot) | ✅ done | b62 |
| Cinematic intro reveal: see building → doors SWING open → dive in → menu | ✅ done (real image; doors swing) | b65 |
| Slow-mo gated on opponent-out-of-range (defender can't reach the ball) | ✅ done (Pass 2 of 2) | b66 |
| Slow-mo Pass 2 — opponent out-of-range detection | 🔜 next | — |
| Steal-as-contest (defense vs dribbling, cooldown) + dribble glue | ✅ done | b55 |
| Locker: holo carousel + card flip + equip flow (BUILD 1) — replaces in-flow picker | ✅ done · ⚠️ optional SQL | b70 (buttons restyled b71) |
| Practice launches straight in with the equipped character (no select screen) | ✅ done | b87 |
| Locker per-card flip (resets on navigate/swipe/leave) | ✅ done | b87 |
| Carousel sort: -pe enders → -jak enders (OVR desc), ALEX last (display-only) | ✅ done | b87 |
| Slow-mo goal-lock camera snaps fast (`SLOWMO_GOAL_SNAP_EASE`) | ✅ done | b87 |
| Toasts use the game style (framed pill + game font); GOAL slam preserved | ✅ done | b87 |
| FIFA-clean design tokens (`--fc-*`, light/premium) established by the Locker | ✅ done | b70 |
| UI remodel phase 1: start menu → **FULL-SCREEN** FIFA-clean lobby (full-bleed, swappable backdrop, hero showcase) | ✅ done | b90→b91 rebuild |
| Mobile lobby overlap fix (scrolls; counter no longer behind character; no compression overlaps) | ✅ done | b92 |
| Lobby showcase: flippable stats banner (front=mechanics, back=record) + LIVING animated character | ✅ done | b93 |
| Lobby polish: free-roaming character (no box) + lobby-only feathered-ellipse face crop + slower ease + deep ball arc | ✅ done | b94 |
| Global (in-match) face-crop — apply the head-only crop everywhere | 🔜 future build | — |
| Lobby social/store corner-icon move (Fortnite corner icons) | ✅ done · b107 (SOCIAL 👥 top-left, STORE 🛍️ top-right bell/X row; same IDs/handlers; hidden in-match) | b107 |
| Local 2-player (desktop, one keyboard) — P1 WASD/Space/F/ShiftL · P2 Arrows/Enter/Slash/ShiftR, aim by facing | ✅ done · b107 (hidden on touch; reuses `mode==='2p'`; fixed shift cross-trigger) | b107 |
| Lobby backdrop (full-screen pitch/stadium) — **swappable** (single `#lobbyBackdrop` rule) | ✅ done | b91 |
| Lobby invite slot (Fortnite squad slot) — **2v2 PLACEHOLDER** ("coming with 2v2") | ✅ done · placeholder | b90 |
| Collection-ready `owned` seam (all owned; rarity/packs later) | ✅ architected | b70 |
| Per-character match stat-tracking (fills Locker card BACK) — BUILD 2 | ✅ done · ⚠️ needs SQL | b72 |
| Per-character CARD tracking (🟨/🟥, 2🟨+1🟥 rule) + profile card totals (styled) | ✅ done | b88 |
| Monotonic per-char stats storage (`merge_char_stats` RPC, GREATEST — no wipe) | ✅ done · ⚠️ needs SQL run | b88 |
| GRADED card model (6 stats /20 → OVR /120, unequal; grabCap varies 48–52) | ✅ done | b74 |
| Half-star ratings (Grab/Hold/Tackle ★/5) on the extended card — display only | ✅ done | b75 |
| Ore-tier card visuals (Stone→Netherite by overall band) — cosmetic only | ✅ done | b75 |
| Diving stat wired → dive/lunge mechanic (right-click / mobile btn, diving-scaled, defender bonus, cooldown+stamina) | ✅ done · b99 right-click = lunge ONLY via document-level contextmenu (menu fully suppressed across play area); menu/pause on Esc | b85 / b99 |
| Wager mode (stake $BASED, peer-negotiated min-stake handshake, settle_match) | ✅ done · shares the relay's single quick-queue by design (opportunistic wager; verified stakes correctly between two wager players) — dedicated wager queue would need a relay `fly deploy` | b99 diag |
| AI lunge DECISION-making (clean ball-first only, yellow caution, dive/difficulty-scaled — CPU rarely fouls out) | ✅ done | b89 |
| Foul detection (lunge collects player-not-ball = foul; clean ball-win legal; tunable threshold) | ✅ done | b85 |
| Yellow/red cards + juiced card flash + per-player HUD mark | ✅ done · b97 fix: counts persist across goal/half-time reset (2🟨→🟥→removal now reliable; can't exceed 2) | b85 / b97 |
| General team-removal rule (red removes player; forfeit at 0 players — 1v1 done, 2v2 auto-slots-in) | ✅ done (1v1 testable; 2v2 scenarios pending 2v2) | b85 |
| Red card ENDS the match → standard REMATCH screen (offline + online, both peers) | ✅ done · b106 (offline was already fine; online guest hung — win was only a loss-tolerant/unordered DC snapshot + a trailing `st:'play'` overwrote it; now gated + reliable WS `{end:who}`) | b106 |
| Foul reset reuses goal-kickoff `reset()`, ball to the fouled side (host-auth, guest mirrors) | ✅ done | b85 |
| Matchmaking by overall (keeps the graded ladder fair within tiers) | 🔜 next | — |
| Crash-proof `EXTRA_CHARS` guard (un-built can't dead-screen) | ✅ done | b50 |
| **TURN server** (P2P through CGNAT/mobile NATs) — Cloudflare via `/api/turn` | ✅ wired (b78) · ⚠️ needs `TURN_KEY_ID`+`TURN_KEY_API_TOKEN` Vercel env vars | b78 |
| P2P handshake diagnostics (`P2P_DEBUG` console + toast) | ✅ OFF for prod (b83); re-enable via flag | b77→b83 |
| **4-player (2v2)** in one match | 🔜 next — multi-phase, not started | — |
| Hourly 0.1 SOL payout backend | 🔜 deferred — banner collects wallets, nothing sends | — |
| More characters from `SOCC\CHARACTERS` | 🔜 on request | — |

---

## Roster (14 characters) — GRADED card model (b74), unequal overalls (ladder 75–100)
`grabCap()` now VARIES per card (GrabR 48–52, visible). `key` = internal id / sprite filename. Front stats each /20: PAC·DEF·STA·DRB·POW·DIV; **OVR** = sum (/120). On/off-ball speed (extended card) derive from PAC. DIV displays but is unwired (dive build later). Names are display-only (keys unchanged).

| key | name | PAC | DEF | STA | DRB | POW | DIV | **OVR** | GrabR | archetype |
|-----|------|-----|-----|-----|-----|-----|-----|------|-------|-----------|
| boomer | ALEX | 18 | 13 | 16 | 18 | 18 | 17 | **100** | 48.5 | Attacking Flagship |
| chad | CHAD | 17 | 12 | 14 | 15 | 19 | 16 | **93** | 48 | Power-pace |
| elon | MUSKPE | 20 | 9 | 14 | 16 | 13 | 18 | **90** | 48 | Speedster |
| pepe | PEPE | 18 | 10 | 13 | 19 | 12 | 16 | **88** | 48 | Speedster-Dribbler |
| pdidpe | PDIDPE | 17 | 10 | 12 | 20 | 13 | 14 | **86** | 48 | Dribbler |
| trumpe | TRUMPE | 9 | 16 | 16 | 11 | 20 | 12 | **84** | 50 | Powerhouse |
| obampe | OBAMPE | 15 | 14 | 14 | 14 | 14 | 13 | **84** | 49 | All-rounder |
| doomer | RUGGEDJAK | 12 | 13 | 15 | 12 | 19 | 12 | **83** | 49 | Powerhouse |
| wojak | WOJAK | 14 | 14 | 14 | 14 | 14 | 12 | **82** | 49 | All-rounder |
| soyjak | SOYJAK | 10 | 17 | 15 | 11 | 18 | 10 | **81** | 51 | Power-Defense |
| okeyjak | OKEYJAK | 13 | 13 | 14 | 15 | 13 | 11 | **79** | 49 | All-rounder |
| sadjak | SADJAK | 12 | 15 | 15 | 12 | 13 | 11 | **78** | 50 | All-rounder |
| oldwoj | OLDJAK | 8 | 20 | 19 | 11 | 18 | 18 | **94** | 52 | Defensive Flagship (Netherite, b76) |
| grandpa | EPPE | 7 | 20 | 17 | 8 | 14 | 9 | **75** | 52 | Defensive Wall |

*(pepe/trumpe sprites are `pepe_cut_s.png`/`trumpe_cut_s.png`; the rest `<key>_cut.png`.)*

Art source library: `OneDrive\Desktop\SOCC\CHARACTERS` (and the `C` subfolder = the b43 refresh batch). More chars exist there to add if asked.
*(How to add a character — the sprite pipeline — is in PROJECT_BRIEF §7 + the memory note.)*

---

## Roadmap / next candidates (detail in PROJECT_BRIEF §11)
- **TURN server** — lets P2P connect through strict/mobile (CGNAT) NATs (currently STUN-only → those fall back to relay). Biggest remaining latency win.
- **4-player (2v2)** — requested, not built. Needs rooms-of-4 in `server.js` (+fly redeploy), 2v2 teams + 4-player render/collision, binary/delta snapshot encoding (~6× bandwidth), star-topology P2P. Multi-phase — defer, don't half-build.
- More characters available in `SOCC\CHARACTERS` on request.

## Watch-outs (live)
- OneDrive actively churns/dehydrates files in this folder (sprites, build.py have gone missing mid-session). Verify before building; `_cut` sprite backups are in the session scratchpad.

---

## Log
- b84: **guest interpolation smoothness fix — choppy even on a good connection.** **Root cause:** guest snapshots are buffered by RECEIVE time (jittery) and rendered at `now − delay`; the adaptive-delay FLOORS were below the buffer the interpolator needs. Host sends 60Hz (16.6ms/snap), but `BALL_DELAY` floored at 20ms (~1.2 snapshots = no cushion) and `INTERP_DELAY` at 44ms (~2.6). Normal receive jitter (always present, even on a good link) pushed the render point past the newest snapshot → `sampleAt` extrapolated (k=1+f, guessed velocity) then SNAPPED back when the late packet arrived → the choppy "guessing". **Fix (guest-side only, all tunable):** (1) raised floors — `INTERP_FLOOR=80`/`INTERP_CEIL=170`, `BALL_FLOOR=48`/`BALL_CEIL=85` — so a steady 60Hz buffer always has 3-5 snapshots of cushion (verified `applyNetState` now interpolates k≈0.16, not extrapolating); adaptive `clamp(FLOOR + rtt*k, FLOOR, CEIL)` still grows under jitter (rtt250→155). (2) `smoothRender(cur,target)` on the opponent + ball — eases only discontinuities (per-frame jump > `RENDER_SNAP_PX=34` → close `RENDER_EASE=0.35`/frame; smaller deltas track exactly → zero added lag in normal play) → kills the snap-back/rubber-band. (3) underrun continues last velocity ≤ `EXTRAP_CAP_MS=100` then holds. (4) host send loop subtracts `SNAP_INTERVAL_S` instead of resetting the accumulator → steadier 60Hz cadence. **Own-player prediction UNCHANGED** — it reconciles to the freshest snapshot, so your own movement stays real-time; only the opponent renders ~80ms back (the bought-latency-for-smoothness trade the task asked for). **Host rendering unaffected, authority unchanged, no desync** (all guest visual-interp). Verified in-browser: floors as defaults, adaptive growth, smoothRender track/ease, no-throw `applyNetState` interpolating. Build clean; harness passes; 0 tokens.
- b83: **P2P_DEBUG off for production.** Flipped `let P2P_DEBUG=true → false` now that TURN is confirmed working (b78-b82). All 12 `[p2p]` handshake console lines (incl. the `console.warn` give-up line) and the diagnostic give-up toast ("No direct P2P — relay mode (NAT needs TURN/STUN blocked)") are gated by `P2P_DEBUG`, so a flag flip silences every diagnostic for real players — no `if(P2P_DEBUG)` block bodies needed removing. Verified in-browser: `P2P_DEBUG===false`, a full `NET.p2pStart` handshake logs **0 `[p2p]` lines**, netcode intact (`loadTurn`/`TURN_SERVERS`/`p2pStart`/`p2pHandle`/`relay`). Pre-existing UX toasts kept (⚡ "Direct P2P connected" only on a real direct link; "Relay mode" only if a live direct link drops — neither shows in a normal relay-only match). **No netcode / TURN-wiring / relay-fallback change** — display-only. Build clean; harness passes; 0 tokens. Re-enable with `P2P_DEBUG=true` (source rebuild, or live in console).
- b82: **FINAL P2P policy — prefer-direct; region WS relay for relay-only matchups (Cloudflare TURN anycast landed a far/slow edge).** User's b81 match: `relay/srflx` via Cloudflare = **450ms, worse than the Fly Sydney relay (~300)** — Cloudflare's TURN allocation hit a distant edge, so b81's "Cloudflare relay beats the WS relay" assumption was false for them. Reverted to: `dc.onopen` keeps P2P ONLY if the selected pair is DIRECT (host/srflx); a relayed pair → `p2pDown()` → stay on the region-selected Fly relay (more predictable). Kept everything else (async load-TURN-before-gather b81, UDP-only + honest ping b80, 6s disconnect-grace b79). **Diagnosis stated to user (this is the real issue, not code):** the ~300 relay floor = their connection's latency to the relay (~75ms+/leg) — the Fly box itself is ~47ms RTT from a healthy line, so it's bufferbloat / a slow last-mile / congested wifi on ≥1 end. <80ms requires BOTH (a) a DIRECT connection (the symmetric-NAT machine getting a cone NAT — UPnP, off-CGNAT, or a hotspot test), AND (b) genuinely low-latency connections (idle ping + bufferbloat test on each end). Build clean; harness passes; 0 tokens. No further relay tweaks — diagnosis is now network-side.
- b81: **P2P — keep the Cloudflare-relay path + load TURN before gathering (fixes b79 over-correction + a TURN-timing race).** User's match log showed selected pair `srflx/relay` (their direct addr → mate's Cloudflare TURN; mate = symmetric NAT) which b79 then THREW AWAY ("staying on the WS relay") → back to the slow Fly box (~300); and `relay:0` locally because the `TURN loaded` line printed AFTER `ICE gathering complete` (loadTurn race). Fixes: (1) **stop declining relayed paths** — a Cloudflare-TURN UDP path (incl. `srflx/relay` single-hop, and even `relay/relay`) goes through Cloudflare's fast anycast edge and beats the single shared-cpu Fly WS relay; b79's prefer-direct decline was wrong for relay-only peers. `dc.onopen` now always `keepP2P()`; the `getStats()` selected-pair check is LOG-ONLY (direct vs Cloudflare-relay). (2) **TURN-before-gather** — `p2pStart` is now `async` and `await loadTurn()` before building the `RTCPeerConnection`, with a re-entrancy `if(this.pc) return` guard; `p2pHandle` is `async` and `await`s `p2pStart(false)` on the offer-first path (+ null-guard) — so a symmetric peer gets its relay candidate instead of `relay:0`. Verified in-browser: async initiator path produces offer+candidates, DC created, `signalingState=have-local-offer`, no throw. b80 UDP-only TURN + b80 honest-ping + b79 6s disconnect-grace all unchanged. Build clean; harness passes; 0 tokens. **Net: the user's `srflx/relay` Cloudflare path is now KEPT (should be far below the old 300; sub-80 depends on the AU edge). Truly-direct host/srflx (both cone-NAT) remains ICE's first pick = lowest ping.** (`P2P_DEBUG` still on; flip false once happy.)
- b80: **toward <80ms — UDP-only TURN + honest ping display.** User pushed back (two SE-QLD peers should be <80ms). Two real improvements: (1) **UDP-only TURN** — `loadTurn` filters the Cloudflare iceServers to stun + `turn:…?transport=udp` only (drops `tcp` + `turns:` TLS). Verified vs the live CF response: kept 2 UDP turn URLs + creds, dropped 4 TCP/TLS. Effect: relay candidates ~12→~4, so ICE doesn't drown the higher-priority DIRECT (srflx) pair in relay candidates during nomination, and if relay IS used it's low-latency UDP rather than the slow TCP/TLS that produced the 500ms. (2) **Honest displayed ping** — the on-screen pill was fed `NET.rtt`, the *asymmetric* EMA (grows fast on spikes, relaxes slowly) that's intentionally biased HIGH to size the jitter buffer → it over-reported. Added `NET.rttDisp` (symmetric 0.7/0.3 smoothing) for the pill; `NET.rtt` still drives INTERP_DELAY/BALL_DELAY. Reset `rttDisp` on transport change (P2P up/down). b79's prefer-direct + 6s disconnect-grace unchanged. Build clean; harness passes; 0 tokens. **Stated to user:** the <80ms target is only physically reachable on a DIRECT connection (both peers cone-NAT); b80 maximises that + speeds any relay + makes the number honest, but if a peer is symmetric-NAT/CGNAT no code can make it direct — the `[p2p] selected pair srflx/srflx` (achievable) vs `relay/…` (stuck) console line is the definitive read, and the network-side fix is router NAT/UPnP, off-CGNAT from the ISP, or one peer on a different connection.
- b79: **P2P stability — prefer-direct path + transient-disconnect grace (fixes 500ms/unplayable after TURN went live).** Once TURN creds were live (`/api/turn` returning Cloudflare ICE), two SE-QLD desktops' console showed P2P **connecting then flapping**: `⚡ DataChannel OPEN` → `connectionState: disconnected` → reconnect, repeatedly, on a **relay** pair (`host:2 srflx:2 relay:12`), reading 500ms+ and unplayable — worse than their Sydney WS relay (~300). Root cause: (a) ≥1 peer is symmetric-NAT/CGNAT so ICE can only use a TURN-**relayed** path, which bounces through Cloudflare and isn't better than the Sydney relay; (b) my handler tore P2P down on every transient `disconnected` (a recoverable state) → thrash. **Fix 1 (prefer-direct):** in `dc.onopen`, `pc.getStats()` finds the selected candidate pair; if either end is `candidateType==='relay'`, `p2pDown()` and stay on the WS relay — keep ⚡ ONLY for true direct (host/srflx) pairs. So cone-NAT matchups get the ~50-70ms direct win; relay-only matchups (like these two) stay on the stable ~300 relay instead of a worse 500 TURN path. **Fix 2 (grace):** `onconnectionstatechange` now only tears down on `failed` immediately; `disconnected` starts a 6s timer that's cancelled if it returns to `connected` (clears in `p2pDown`/`p2pReset`). Verified pair-detection on a host↔host loopback → `relayed:false` → keeps P2P. b78 TURN loader unchanged; relay fallback / snapshot sync / matchmaking unchanged; build clean; 0 tokens. **Honest limit told to user: two symmetric-NAT peers physically can't form a direct connection — the relay is their floor; the ⚡ win needs ≥1 peer on a normal (cone) NAT.** `P2P_DEBUG` still on to confirm; flip false after.
- b78: **Cloudflare TURN wired via serverless `/api/turn` (implements the b77 fix).** The b77 diagnosis (STUN can't traverse CGNAT/symmetric NAT → needs TURN) is now solved properly. **New `api/turn.js`** (Vercel): on GET, POSTs to Cloudflare `…/v1/turn/keys/{TURN_KEY_ID}/credentials/generate-ice-servers` with `Bearer TURN_KEY_API_TOKEN` (ttl 86400) → returns `{iceServers:{urls,username,credential}}`; the key secret lives ONLY in Vercel env vars, never in the client. On any failure / unconfigured → returns `iceServers:null` (HTTP 200) so the client cleanly stays STUN-only. **Client:** `TURN_SERVERS` is now `let`; `loadTurn()` GETs `/api/turn`, sets `TURN_SERVERS=[iceServers]` (cached ~1h, creds are short-lived), and is called at the top of `NET.connect` so it runs in parallel with the WS connect — ready before `p2pStart()` spreads it into the `RTCPeerConnection` iceServers. Verified in-browser: missing `/api/turn` → `loadTurn` doesn't throw, `TURN_SERVERS` stays `[]`, STUN-only, nothing breaks; iceServers still constructs; a real CF response maps to the correct `[{urls,username,credential}]`. `P2P_DEBUG` logging from b77 stays on to confirm ⚡ once live. Relay fallback / snapshot sync / matchmaking unchanged. `api/turn.js` + game both syntax-pass; harness OK; 0 tokens. **⚠️ ACTION (user, one-time):** create a Cloudflare Realtime/Calls **TURN key** (`uid` = `TURN_KEY_ID`, plus its API token), set `TURN_KEY_ID` + `TURN_KEY_API_TOKEN` in the Vercel project env vars, redeploy → CGNAT peers then connect P2P (⚡, ~50-70ms). Set `P2P_DEBUG=false` after confirming.
- b77: **P2P latency diagnosis — STUN can't traverse CGNAT, needs TURN (diagnostic logging + TURN scaffold added; NOT a code bug).** Symptom: two SE-QLD home-wifi DESKTOPS on the Sydney relay show steady 300-400ms, no ⚡ (DataChannel never opens → every packet relays through the Fly box). **Investigated all four angles the task named:** (1) traced the handshake — `p2pStart` host=initiator (createDataChannel+offer), guest answers, trickle ICE via `onicecandidate`→`signal({c})`; `p2pHandle` does setRemote→createAnswer, queues candidates in `candQ` until `remoteSet` then `drainCand` — **correct**. (2) STUN config = Google×3 + Cloudflare, constructs fine. (3) Signalling path: `signal`→`send({t:'relay',d:{rtc}})`→ relay server `m.t==='relay'` forwards to the other peer BOTH ways → `onRelayData` checks `d.rtc` FIRST → `p2pHandle` — **forwarded reliably, no drop**. (4) No regression: sm/cm/cs/goalSeq (snapshot payload), charStats, and the region fix never touch the rtc routing; `d.rtc` is the first branch in `onRelayData`. **Proof the code is correct:** an in-page loopback test ran the exact offer/answer/candidate pattern between two RTCPeerConnections → DataChannel **opened**, ICE `connected`. **Conclusion (root cause):** STUN-only can establish P2P through CONE NATs but **CANNOT traverse SYMMETRIC NAT / CGNAT** — ubiquitous on Australian residential (many NBN RSPs + all 4G/5G home internet). With either peer on CGNAT, ICE finds no working candidate pair → permanent WS-relay fallback → the 300-400ms. **This is a genuine NAT case; the honest fix is a TURN server (not a code change).** Shipped: (a) `P2P_DEBUG=true` diagnostic logging — ICE state transitions, local+remote candidate TYPES (host/srflx/relay), DC-open, and a plain-English give-up verdict ("srflx gathered but no pair → symmetric/CGNAT → needs TURN" vs "no srflx → STUN blocked"), plus an on-screen toast so the user can confirm live without F12; (b) `TURN_SERVERS=[]` scaffold spread into `iceServers` (empty = STUN-only, behaviour unchanged) — paste Cloudflare TURN creds (`{urls:[turn:...],username,credential}`) to enable. Relay fallback, snapshot sync, and matchmaking all unchanged. Build clean; harness passes; 0 tokens. **NEXT: supply Cloudflare TURN creds → `TURN_SERVERS` → expect ⚡ + ~50-70ms.** (Set `P2P_DEBUG=false` once confirmed.)
- b76: **Oldjak (oldwoj) elite-defender correction — OVR 77 → 94, now Netherite.** Bug: `oldwoj` was the budget/Stone OVR 77, but he's the intended ELITE DEFENDER (defensive counterpart to ALEX). Fix = one `STATS` row: `pace8 def20 sta19 drb11 pow18 div18` (was `8/19/16/9/16/9`), grab 52 unchanged → **OVR 94**. Single source of truth means the one edit updates the locker card (OVR headline, 6 front stats, grab/hold/tackle stars, ore tier) AND gameplay (steal contest reads def 20 / drb 11, grabCap 52, kickPow from pow 18). He auto-lands **Netherite** via the existing `oreTier` (94 ∈ 94–100) — **no manual tier override** (function untouched, still purely overall-driven). Netherite tier is now ALEX(100) + OLDJAK(94). Verified: OVR 94, tier netherite, **Tackle 5★ (def20) + Grab 5★ (grab52) intact**, grabCap 52 in-game, pace 8 → still slow wall; all 13 other cards unchanged (re-checked vs the doc table — only oldwoj diverged). Also relabeled archetype "Defensive Wall" → "Defensive Flagship" to mirror ALEX's "Attacking Flagship" (EPPE keeps the budget "Defensive Wall"). The design-doc table still shows oldwoj 77 — stale; this is the approved correction. Build clean; harness passes; 0 tokens.
- b75: **locker visuals — half-star ratings + ore-tier card cosmetics (the two b74-spec parts that were missed).** DISPLAY-ONLY; **no stat/overall/grabCap/gameplay change** (verified grabCap 48.5/52 + OVR 100/75 identical to b74). **PART A — half-stars:** the extended card's Grab radius / Hold-ball / Tackle win rows now render ★ out of 5 (half allowed) instead of raw `48.5` / `84%` / `27%`. Mapping (nearest half): `grabStars=grab-47` (48→1★ .. 52→5★), `holdStars=drb/4`, `tackleStars=def/4`; `starHTML` emits full/half/empty `.loStar` spans (half = a 50%-clipped gold ★ overlay). Gameplay still uses the real values. Verified: Eppe/Oldjak grab **5★** + tackle **5★**, attackers grab **1★**, Pdidpe/Pepe hold **5★** (DOM: full,full,full,full,full); on/off-ball stay numbers. **PART B — ore tiers (cosmetic, by OVERALL band, no power):** `oreTier(c)` from `overallOf` → Stone 75-79 / Iron 80-83 / Gold 84-87 / Diamond 88-93 / Netherite 94-100; the carousel slot gets `tier-X`, styling the front frame border + material gradient + colored glow + top accent stripe. A `.loSheen` overlay does the animation — transform-translateX shine-sweep for Gold/Diamond, purple opacity shimmer for **Netherite** (dark charcoal+purple card with light-text overrides, the special one) — animated **only on the active card** (compositor-friendly transform/opacity) so mobile FPS is safe. Equipped indicator switched from box-shadow ring to `outline` so it stacks with the tier glow. Reuses the b70 `owned`/cosmetic-rarity seam. Verified bands: ALEX(100)=Netherite, CHAD/MUSKPE/PEPE=Diamond, PDIDPE/OBAMPE/TRUMPE=Gold, WOJAK/RUGGEDJAK/SOYJAK=Iron, OKEYJAK/SADJAK/OLDJAK/EPPE=Stone — escalation obvious (Netherite ALEX vs Stone Eppe). **NB:** the spec's "Oldjak Netherite" example is a typo — Oldjak OVR 77 → **Stone**; implemented strictly by the "auto-compute from overall" rule. Build clean; harness passes; 0 tokens.
- b74: **card grading / stat rework — graded model replaces equal-totals.** Per the approved `BASED_CUP_GRADING_DESIGN.md` table. **Model:** 6 FRONT stats each /20 (Pace·Defending·Stamina·Dribbling·Power·Diving) → **OVERALL /120** = their sum; cards now have DIFFERENT overalls (ladder 75 EPPE → 100 ALEX). `STATS` rows are now `{name,abbr,arch,pace,def,sta,drb,pow,div,grab}` — the ONE table locker + gameplay read. **Card UI:** front shows OVERALL top-left (replacing Pace) + the 6 stats /20 (bar width ×5); extended/back shows the granular breakdown — on-ball+off-ball speed (derived via `speedSplit`, sum EXACTLY to Pace), **grab radius (visible)**, **Hold-ball %** + **Tackle-win %** (derived from the SAME `stealChance(def,drb)` the in-game contest uses, vs a `CONTEST_AVG=14` baseline, so the shown chance matches in-game — verified grandpa tackle 51% == in-game p 51%), archetype, plus the b72 record (scrolls). **grabCap now per-card:** `grabCap(pl)=pl.grab` (48–52 from the table, stored via `statDerived`/`mkPlayer`) — the ONE place the hitbox varies, tight + visible; equal-totals fairness is intentionally replaced by graded cards + (later) matchmaking-by-overall. **Gameplay rewire (all read the new stats, /20-rescaled):** movement from the on/off split (`SPEED_FLOOR/SLOPE` on ~/10 halves); steal tackle-success from `def` & hold/retention from `drb` (`STEAL_WEIGHT` 0.06→0.035); closing bonus `DEF_CLOSE_BONUS` 0.06→0.03; glue from `drb/20`; `kickPow=KICK_BASE(3)+pow*KICK_SLOPE(0.75)` (~12–18, same shot feel); stamina drain/regen rescaled. Power still charge+power only. **Diving** is displayed (DIV) but NOT wired to any mechanic (dive/lunge build reads it later). **Renames (display only):** doomer→**RUGGEDJAK**, oldwoj→**OLDJAK** — internal keys/sprite files/STATS lookups unchanged (verified sprites load + stats resolve). archetype moved into STATS (`CHAR_META` removed → single source). Locker exit button → smooth top-left **"‹"** icon. Verified in-browser: all 14 OVR+grab match the table, splits sum to pace, grabCap varies, contest %=card %, gameplay smoke test (movement/steal/grabCap, no errors), front + extended render, "‹" present, mobile fits. Build clean; harness passes; 0 tokens. **NOT this build:** dive mechanic, matchmaking-by-overall, grading tiers/packs.
- b73: **fix — menu overlay shown on top of the live match (mobile-reported; quick & wager).** Diagnosed first (changed nothing): reproduced at 375px and listed visible `.overlay`s after match start — QUICK left `modeSelectScreen` visible, WAGER left `wagerSelectScreen` visible, both with `state==='play'`; PRACTICE was clean. **Root cause:** `startQuickMatch` and the begin functions (`_beginGameOnline`/`_beginGame`) dismissed a HARDCODED list of overlays that **omitted the PLAY-hub (`modeSelectScreen`) and wager (`wagerSelectScreen`) screens**. The search screen (`matchScreen`) covered the hub only during "Searching…"; once the match began and `matchScreen` was hidden, the never-hidden hub overlay re-surfaced over the canvas (`.overlay` = z-index 20, near-opaque background, above the game; on mobile it also captured touches / blocked the joystick). Practice escaped it because `openSelect` routes through `hideAllOverlays`. **Fix:** match-start now calls `hideAllOverlays()` (hides EVERY `.overlay`) in `_beginGame`, `_beginGameOnline`, and `startQuickMatch` (before showing the search screen) — robust against any future menu screen too. In-match chrome is unaffected (`#hud`, `#touch` joystick, `.pcard` are NOT `.overlay`; still shown by `enterMatchChrome`). Verified at 375px: quick/wager/practice each start with **0 visible menu overlays**, HUD + cards present, pause opens/closes, screenshot shows a clean pitch. (Reproduces on desktop too; the "mobile" report is because it's mobile-first and the overlay blocks the touch controls.) grabCap 48; build clean; harness passes; 0 tokens.
- b72: **Locker BUILD 2 — per-character, per-player match stat tracking (observe-only).** (`LOCKER_STAT_DEFINITIONS.md` was not on disk — searched repo/Downloads/Desktop/git; built to the task-body event definitions, will reconcile if the doc appears.) Hooks observe EXISTING events and record — **no gameplay/steal/scoring/grabCap change**: (1) the steal contest in `updatePossession` now captures its outcome (`stolen = Math.random()<p`, computed identically to before) and calls `recordContest(carrier, defender, stolen)` — ONE pressure event feeds the carrier's **dribble** (success = kept the ball) and the defender's **tackle** (success = won the ball), tracking numerator AND denominator for both rates; (2) `scoreGoal` → `recordGoalStat(who)` = scoreboard-consistent goals-for/against + an **own-goal** sub-flag when `ball.shooter` is the conceding side; (3) `endGame` → `commitMatchStats(who)` records W/L. A per-match `matchStat{p,t}` accumulates ONLY on the authoritative side (host + offline; the guest's thin client never runs `updatePossession`/`scoreGoal`/`endGame`, so it can't double-count) and commits via `commitCharStats` → local `sh_crec_<user>` + guarded Supabase `char_stats` jsonb mirror. **Online attribution / no double-count:** the host commits ITS OWN char (`players.p`) and **relays the GUEST its own deltas** (`players.t`) via a `{charStats:{char,rec}}` message; the guest applies it in `onRelayData` to its own profile — each player's stats land once. Verified in-browser: dribble 67% (2/3), tackle 50% (1/2), gf2/ga2/og1/W1 correct; accumulates across matches; host commits pepe + relays elon (NOT committing elon locally); guest applies relayed elon; card BACK shows real numbers. **Bug fixed (from b70):** `charRecordAll()` did `JSON.parse(DB.get(...))` but `DB.get` already parses → it always threw → returned `{}`, so the card back could never display data; now reads the parsed object directly. **Scope (decided + stated):** tracks every real match WITH an opponent — online + practice-vs-CPU + local 2P; **solo excluded** (no opponent). For online-ranked-only, change `statTracking=oppActive` → `oppActive && ranked`. **⚠️ SQL REQUIRED for cross-device (local works without it):** `alter table profiles add column if not exists char_stats jsonb;` Build clean; harness passes; 0 tokens; grabCap 48.
- b71: **locker button restyle (UI/CSS + label only — no logic).** EQUIP button: label "EQUIP <NAME>" → just **"EQUIP"** (`positionLocker`), and off-green → a FIFA-clean **navy-ink** CTA (`.loEquip` = `linear-gradient(180deg,#27517a,var(--fc-ink))`, white text, hover lift) so green is reserved for the EQUIPPED state. Back/nav: `.loBackBtn` (exit-to-menu) + `.loArrow` (carousel nav) given the matching secondary treatment (white `--fc-card` surface, `--fc-line` border, `--fc-ink` text, accent-on-hover) so they read as one family with the CTA instead of the old UI. EQUIPPED-state styling unchanged and still obvious (green ring + ✓ badge + green-outline "✓ EQUIPPED" button — verified `borderColor rgb(16,163,90)`). No change to equip logic/persistence, the carousel, the flip, or the card data; verified equip still works and desktop + 375px mobile both render clean. Build clean; harness passes; 0 tokens; grabCap 48.
- b70: **Locker BUILD 1 of 2 — holographic carousel + equip flow + card flip (FIFA-clean design tokens).** (`BASED_CUP_LOCKER_DESIGN.md` was missing at build time so I built to the task-body spec + `BASED_CUP_STAT_DESIGN.md`; the user added the doc afterward and the build was **verified to conform** — its 3 open calls were already decided by the build prompt: carousel, top-level button, card-back record.) Save point `ui-v1-classic` tag confirmed present. **What shipped:** (1) `#lockerScreen` overlay — a floating holographic carousel of all 14 chars; active card centred+large, adjacent cards' edges peek (depth cue), with swipe/drag (pointer events, tap-vs-swipe threshold), ‹/› arrows, dot nav, and a gentle float anim. (2) Two-sided flip card (tap the centred card): FRONT = head art + name + archetype (`CHAR_META`, from the stat-design doc) + 6 stat bars (on/off/power/defense/dribble/stamina) + **PACE** headline — all read from `STATS` (single source of truth, zero duplicated numbers); BACK = your record with that char (W/L, goals scored/conceded, own goals, dribble%, tackle%) via `getCharRecord()` which returns 0/"—" until BUILD 2 populates the store. (3) **EQUIP** sets `equippedChar`+`humanChar`, persists local (`sh_equip_<user>`) + mirrors to Supabase `profiles.equipped` (guarded — see SQL), shows a ring + ✓ badge on the equipped card, defaults pepe. (4) **Per-match picker removed**: quick/wager set the equipped char and queue straight in (no select screen); practice/host keep their mode/opponent options but the character carousel is hidden and shows "Playing as <name>"; the online guest now plays its own equipped char (`setupGuestWager` → `guestChar=equippedChar`, friend mode no longer forces the opposite). (5) Menu **🎽 LOCKER** button (near PLAY) shows the equipped name. (6) **`owned` seam**: `isOwned(c)` (all true now) gates carousel render/equip + a lock layer — architected for rarity/packs, not built. **FIFA-clean tokens** (`--fc-bg/ink/accent/gold/card/...` scoped to `#lockerScreen`) establish the new light/premium design system for the upcoming UI remodel without regressing the dark UI. **Fairness:** display+selection only — `grabCap` constant 48, no gameplay/stat/hitbox change. Verified in-browser (desktop 956px + mobile 375px): 14 cards, active centred / neighbour peeking, front stats from STATS (pepe pace 17), back 0/"—", flip both ways, equip→persist→survives reload, all flows use equipped, old carousel hidden, menu name updates; screenshots confirm the look. Build clean; harness passes; 0 tokens/placeholders. **⚠️ OPTIONAL SQL (cross-device equip; local works without it):** `alter table profiles add column if not exists equipped text;` **BUILD 2 (next) = per-character match stat-tracking** to fill the card back.
- b69: **keep-away aim-arrow removed + leaderboard reworked to Hourly + All-Time.** **PART 1** (keep-away): gated the two `drawAim(players.p/.t…)` calls in the render loop with `if(gameMode!=='keepaway')` — keep-away (a possession mode) no longer shows the shot-aim arrow; goals mode unchanged; the steal contest (defense-vs-dribble, cooldowns) is fully intact (it lives in `applyMove`/possession, never in the draw). Verified: goals→1 aim call, keep-away→0. **PART 2** (leaderboard) — **DIAGNOSIS:** one backend (Supabase `match_results` table + RPCs `leaderboard`/`player_rank`, written/read via Vercel `api/result.js` + `api/leaderboard.js`; `LB` client calls `/api`; local-accounts cumulative is only the offline fallback). The "12 hour" board was a **ROLLING** window (`created_at >= now() - interval '12h'`), sliding continuously — **not** a reset/rollover and not clock-aligned. "Winner" = `sum(won) group by username` in the window. **IMPLEMENTED:** two tabs in the board screen — **⏱️ This Hour** (default; current Brisbane/AEST clock hour, **hard reset on the hour**) and **🏆 All Time** (cumulative forever). Hourly filters `created_at >= (date_trunc('hour', now() at time zone 'Australia/Brisbane') at time zone 'Australia/Brisbane')` → at each AEST :00 the boundary jumps forward so the prior hour drops out and the board reads zero — **purely a query boundary, NO row is ever deleted/mutated**. All-Time aggregates the same table with no time filter. **HARD-RULE proof — hourly reset can't touch all-time:** all four functions are `language sql stable` SELECT-only (no DELETE/UPDATE/TRUNCATE anywhere); all-time has no `where` at all, hourly's "reset" is just its moving `where created_at >=`; both read the same immutable `match_results`. Code: `LB.boardMode/rankMode(mode)`, `api/leaderboard.js` routes `?mode=hourly|alltime` to the new RPCs (legacy rolling kept when no mode), `renderBoard(mode)` + `seg('boardTabs',…)`, home-menu top-3 → hourly ("this hour" label). **⚠️ SQL REQUIRED (user must paste into the Supabase SQL editor — NOT auto-applied):** 4 functions `hourly_leaderboard`, `hourly_player_rank`, `alltime_leaderboard`, `alltime_player_rank` (added to `supabase-schema.sql`; zero-comment paste block given in chat). Until run, online board returns error → UI shows "needs a connection"/local fallback. Build clean; harness passes; 0 tokens; grabCap 48.
- b68: **US region disabled (its relay was never deployed) + STUN redundancy.** Diagnostic of the live infra: `basedcup-lobby-syd` and `basedcup-lobby-sin` are both up with exactly 1 machine each (in-memory quick-match queue intact, not split; HTTP "lobby ok"; measured WS app-RTT 47ms Sydney / 142ms Singapore from AU). `basedcup-lobby-us` **does not exist** (`fly apps list` shows only syd+sin) yet `index.html` exposed a 🇺🇸 US button + server-popup row pointing at `wss://basedcup-lobby-us.fly.dev` → selecting US = silent failed connection (looked like "matchmaking broken"). Fix (code-only, no infra): `REGION_SOON={us:true}` → US greyed "coming soon" in both the segment and popup, `setRegion('us')` toasts and no-ops, `curRegion()` never returns a soon region, `refreshServerPopup` skips pinging it, `NET.connect` rewrites the dead US URL to `DEFAULT_LOBBY` (covers old invite links), and a one-time migration moves any saved `sh_region=us`/US `sh_lobby` back to Sydney. Lag: added redundant STUN (Google ×3 + `stun.cloudflare.com`) for higher direct-P2P success; documented that there's still **no TURN**, so symmetric-NAT/CGNAT users keep falling back to relaying game traffic over the WS lobby (graceful but laggier) — wire TURN into `iceServers` when a provider is set up. Verified all guards in-browser; build clean; harness passes; grabCap 48. Re-enable US: delete it from `REGION_SOON` once the US relay app is deployed.
- b67: **intro door overlay lines up with the painted door (any screen aspect).** Root cause: b65/b66 positioned the swinging-door overlay with fixed viewport-% (`--dx/--dy/--dw/--dh`), but the reveal image is `object-fit:cover` — scaled + cropped differently per aspect — so the overlay drifted off the actual door. Fix: `layoutIntroDoors()` computes the cover scale `s=max(VW/1600,VH/900)` + centering offset, then sets `#introDoors` left/top/width/height (and `#introReveal` transform-origin for the dive) from the door's IMAGE-fraction box in viewport px; called on intro show + on `resize` (listener removed at finish). Door box `INTRO_DOOR={cx:0.490,cy:0.724,w:0.135,h:0.356}` measured from the user's red-outlined reference (red bbox on the 1672×941 image → fractions; confirmed landing on the door in the baked `intro_reveal.jpg`). Verified: door tracks to 50%/70% (desktop ~square) and 46%/72% (375×812 portrait) — i.e. follows the painted door under each crop. grabCap 48; build clean; harness passes. (Screenshot renderer was unresponsive this session; relied on static overlay + cover-math.)
- b66: **slow-mo Pass 2 — out-of-range gate (defender can't reach the ball).** Added one condition to when slow-mo arms (camera/explosion/decider-gating/netcode/score all UNCHANGED). `shotIsPlayerUnstoppable(side)`, evaluated ONCE as the fast goal-bound shot enters the zone: time-to-cross = (goalLineX − ball.x)/ball.vx; the DEFENDER (L goal→`players.p`, R→`players.t`) is checked against 11 samples of the projected path — can they get within `grabCap`(48) of any path point before the ball arrives, at speed `maxOff + DEF_CLOSE_BONUS·defense` × `SLOWMO_DEF_SPEED_K`, beating it by `SLOWMO_REACH_MARGIN`? If yes → STOPPABLE → no slow-mo; if no → arm. Posts/walls: suppress only a CLEAR wide miss (path exits the mouth by > `SLOWMO_POST_GRACE=34`); a near-post graze still arms (drama). `FORCE_GOAL_SLOWMO` bypasses it. Decided once at entry so a defender catching up mid-flight doesn't disarm. Verified in-browser: defender-close→no, far→arms, post-graze (yAtLine 435 vs GY1 425)→arms, wide-miss (yAtLine 525)→suppressed, FORCE→arms, stays-armed-on-late-catchup→true; grabCap still 48; build clean; harness passes.
- b65: **intro doors actually swing open (rectangular; the real painted door stays).** b64's "doorway opening" just grew a black hole. Now the leaves are **invisible when closed** (you see the actual painted arched door — no fragile colour/width match), and on `.opening` a dark interior (`#introVoid`) appears + two dark **rectangular** door leaves swing out (rotateY, hinged on the outer edges), positioned on the door body below the black lintel beam (`--dx:50% --dy:72% --dw:17% --dh:30%`, tunable). Then `.dive` zooms into the dark opening → menu. Verified by screenshot: closed = real painted door; open = rectangular dark interior + leaves swung to the sides (frame/hinges visible). Also: image stays cover-fullscreen. Front-end only; grabCap 48.
- b64: **intro reveal — baked the real building + reworked to "enter the building."** Feedback on b63: "can't see the image, it's just a door." Two causes: the new building image was never on disk (b63 built with the old reveal), and the b63 fake brown door-leaf panels (`#introLeafL/R`) sat over the centre and dominated. Fixes: (1) found the pasted image at `Downloads/ChatGPT Image Jun 30, 2026, 01_44_06 PM.png`, converted it to `intro_reveal.jpg` (1600×900, quality 86) and rebuilt — the blue/white/gold dome building now renders. (2) Removed the door-leaf panels; the building image is now the visible star (brighter start: `brightness(.4) blur(5)` → `.lit` full). The "doors open" is now `#introVoid` — an **arched dark shape positioned exactly on the doorway** (`--dx:50% --dy:67% --dw:13% --dh:38%`) that grows from a thin door-seam slit to full (`.opening`), reading as the doors parting to the dark interior — then `.dive` (scale-9 toward the doorway) + `#introBlack` swallow the screen → menu. Verified by screenshot: building full+bright, void opens precisely over the doors (arch matches). Kept tap-skip/safety/once-per-session/fallback. grabCap 48; build clean; harness passes.
- b63: **new cinematic intro reveal (front-end only).** Reworked `showIntroLoading` from "black curtain rises → hold on reveal image" to a 4-beat sequence: bag/hood YANKED off the lens (`#loadingScreen.bagoff` keyframe) + building snaps from dim/blur into focus (`#introReveal.lit`) → building's double doors swing open (`#introDoors`/`.introLeaf` rotateY perspective) revealing a black void (`#introVoid`) → fast DIVE through the doorway (`#introReveal.dive` scale-8 with transform-origin at the doorway) as `#introBlack` fades to full → darkness complete → `finish()` into the second menu. Doorway position is 4 tunable CSS vars on `#introReveal` (`--dx/--dy/--dw/--dh`). Kept tap-to-skip, the hard safety backstop, the once-per-session `introPlayed` guard, and the no-image `showLoadingThen` fallback. Verified: full chain completes (callback into menu fires) and resets cleanly (introReveal/loading hidden, doors closed, black off — no stuck zoom). grabCap 48; build clean; harness passes. **NOTE:** the pasted building image isn't on disk — CC can't write a pasted image; user must save it as `intro_reveal.jpg` + rebuild, and likely nudge `--dy` to align the doors.
- b62: **slow-mo camera target fix (camera only — timing/gating/explosion untouched).** b61 eased the focus toward the live `ball.x` generically → it lagged the moving ball and framed the wrong spot ("you can't see the ball go in"). Now `maybeArmSlowmo` decides the framing ONCE when slow-mo first arms, from where the shot was struck (`ball.kickX`, set in `tryKick`) relative to halfway (W/2): **long-range** (struck from own half / beyond halfway) → `slowmoCamMode='ball'` = camera FOLLOWS the ball all the way in; **short-range** (struck inside the opponent's half holding the target goal) → `slowmoCamMode='goal'` = camera LOCKS on that goal (`FIELD.l`/`FIELD.r`) so you clearly see the ball cross. `slowmoCamSide` = the goal scored on. During the explosion it frames the goal. Doesn't flip mid-slow-mo (only set when `slowmoT<=0`). Online-synced: streamed `cm` (mode) + `cs` (side) in the snapshot, consumed into `netCamMode`/`netCamSide` (same proven pattern as `sm`/`goalSeq`); camera is purely visual — never touches netcode/clock/score/collisions. New tunables `SLOWMO_ZOOM_EASE=0.14`, `SLOWMO_FOLLOW_EASE=0.35`. Verified: long→follow, short→lock (both goals, focus = exact goal X), no mid-flip, clean reset (camZoom→1, recenters W/2), mobile-safe (canvas transform leaves DOM/HUD unchanged, fits viewport, draws OK). grabCap still 48; build clean.
- b61: **slow-mo smoothness fix + cinematic zoom.** The b60 frame-pulldown (run the sim only ~30% of frames) made the ball jump every 2-3 frames → felt like buffering/lag. Replaced with a per-frame motion scale `motionScale` (eased toward `GOAL_SLOWMO_SCALE` during slow-mo) applied to the position integration in `applyMove`/`applyMoveAI`/`stepBall`/`predictGuestSelf` — the ball now moves a small amount EVERY frame (60fps) = smooth. Verified: 8 consecutive steps all move (~2.85px avg vs 9.49 normal ≈30%), no zero-deltas. Added a **cinematic zoom**: `camZoom` eases toward `SLOWMO_ZOOM`(1.5) with focus easing toward the ball (or `goalFx.gx` during the blast), clamped so the view never leaves the field, eased back to 1.0 after — verified zoom 1.49 in / 1.0 out, focus tracks the goal, on-field. `maybeArmSlowmo` now keeps slow-mo topped while the shot is in the zone (whole crossing stays slow). Gating, the streamed `sm` sync, the explosion, and "visual-time-only" (match clock/netcode/score untouched) are all unchanged from b60. grabCap still 48; build clean.
- b60: **goal slow-mo — anticipation timing + decider gating (Pass 1 of 2; explosion untouched).** CHANGE A: slow-mo now ARMS as a fast goal-bound free shot enters `SLOWMO_ZONE` (110px) in front of the mouth moving ≥`SLOWMO_BALL_SPEED` (6px/f) — `maybeArmSlowmo()` — so the ball crosses in slow-mo, then the explosion lands; if it doesn't score, slow-mo just eases off. CHANGE B: `slowmoGateOk()` arms only when **2nd half AND last `SLOWMO_LAST_SECS`(20)s AND |pre-goal score margin| ≤ `SLOWMO_MARGIN`(1)** — covers a tying goal, go-ahead-from-tie, and leader-by-one; up-by-2+ does NOT. `scoreGoal` also tops up slow-mo for any gated goal (margin read pre-increment) so tap-in deciders slow too. CHANGE C: `let FORCE_GOAL_SLOWMO=false` debug flag → every goal slow-mos (flip in source+rebuild, or live `FORCE_GOAL_SLOWMO=true` in console). **Mechanism (reading flagged):** "FX clock only" → visual-time-only. Slow-mo dilates the rendered sim via a frame-pulldown (advance the sim ~30% of frames during the window) PLUS the FX clock; it never touches the match clock (`timeLeft` decrements real-time every frame), netcode protocol, or scoring/half outcome. **Online sync:** streamed `sm` flag (snapshot, like `goalSeq`) — host pulls down its authoritative sim (guest sees slow via snapshots) + guest pulls down its own prediction from `sm`. Verified in-browser: gate cases (H1 no / mid-clock no / last-20s tied+margin1 yes / margin2 no), arming needs in-zone fast goal-bound ball, FORCE arms every goal, explosion always plays (gated or not), frame-pulldown ball displacement ratio 0.31≈scale, grabCap still 48, 0 console errors.
- b59: **goal juice (explosion + slow-mo) + loading cleanup.** Restore point first: tag `ui-v1-classic` = commit `5ca3519` (b58), pushed to origin (roll back: `git checkout ui-v1-classic`). **TASK A** — on a goal, `triggerGoalFx(side)` blasts both heads + the ball AWAY from the scored-in goal mouth (outward dir, speed scaled by proximity), plus a shockwave ring + white flash at the goal, screen shake (`shakeAmp`, decays), GOAL slam-in text (reused `titleSlam` feel → `.toast.slam`/`goalSlam`), and `sfxBoom()` (synth sine sweep + noise roar). Purely visual: it draws debris over the frozen scene and **never moves the authoritative players/ball** — score/half/reset run exactly as before. **Online sync:** `goalSeq`+`goalFxSide` ride the snapshot (`gq`/`gsd`, same proven pattern as the rebound `rq`); host increments + detonates locally, guest detonates when `gq` changes; reset to 0 in `newGame` so a fresh guest never spuriously fires. **TASK B** — brief slow-mo beat: the loop scales the FX clock by `GOAL_SLOWMO_SCALE` for `GOAL_SLOWMO_MS` (scales the explosion's opening, NOT the netcode/timers); `GOAL_SLOWMO_SCALE=1.0` disables it. **TASK C** — removed the generic `> based.cup booting…` boot line from the one-time terminal intro (`showIntroLoading`); kept the `> escorting goyim into the system…` flavour + the static loader text (`#loadText` "Escorting goyim into system…"); guest-naming + gameplay text untouched. Verified in-browser (3 debris + 40 sparks, blast away from goal, no errors). grabCap still constant 48; build clean.
- b58: **re-normalise sprites on FACE WIDTH, not bbox height (`process_sprites.py` only).** b57 normalised total head height, but the eye reads FACE size → heavy-hair chars (wojak/doomer/sadjak) got small faces, bald/big-face chars (pdidpe/elon/obampe) looked oversized; diagnosed cheek width varied **31%** (202–281px) while height was uniform. Fix: switched the scale basis back to **`FACE_W=240` cheek width** (perceived-size metric) with `ADJUST={boomer:0.85, okeyjak:1.20, pdidpe:0.88}` (bald-cranium/3-4 misreads), `TOP_MARGIN`/`SIDE_MARGIN` fit-safeties so no hair/face clips, bbox-centred. **Kept b57's okeyjak `CHIN_OVERRIDE=0.88`** so its chin stays uncropped. Now faces read consistently sized (verified via a 14-head render grid — bald, haired, 3-4, ADJUST'd all compared) and silhouette HEIGHT varies by hairstyle (244–350px) = correct. **0 faces cropped** (edge-test + visual), all 14 load 360×450, grabCap still constant 48, no gameplay/stats touched. Old b57 sprites backed up in scratchpad `cut_backup_b57/`.
- b57: **sprite head-size + crop fix (`process_sprites.py` only — no gameplay change).** Two visual defects: (1) heads rendered at different visual heights (cheek-WIDTH was normalised but hair extended freely → in-game head height 57–83px); (2) okeyjak's chin cropped — its bald cranium is the widest row, so the jaw-narrowing chin-detector fired at the mouth (frac 0.79) and `crop(...,chin)` sliced off the real chin (~frac 0.86–0.88). Fix: normalise by **visible HEAD HEIGHT** (hair-top→chin → `TARGET_HEAD_H=268`, ≈ old median 264.5 so most chars barely moved), centre by the head's own horizontal bbox, `SIDE_MARGIN=16` width-contain so nothing clips the sides, and `CHIN_OVERRIDE={'okeyjak':0.88}` for the one auto-detect failure (old per-char width `ADJUST` reset to `{}`). Re-cut all 14, rebuilt. **Verified:** in-game rendered head height now 74–75px for all 14 (spread 1px, was 26px), 0 cropped (edge-test + okeyjak full-face confirmed visually), all 14 load 360×450. grabCap still constant 48; stats/steal/glue/movement untouched. Old `_cut` sprites backed up in scratchpad `cut_backup_b56/`.
- b56: **pace-spread tuning (speed mapping only).** b55's on/off split read as too subtle (~6%). Widened the stat→speed mapping in `statDerived` only: `4.2 + stat*0.16` → `SPEED_FLOOR(2.1) + stat*SPEED_SLOPE(0.50)` (lower floor, ~3× slope, pivots ~stat 6 ≈ old mid-pace). Result (verified via live statDerived): elon on 6.10/off 7.10 (gap 0.32→1.00, ~3.1×), pepe on 6.60/off 6.10 (gap 0.16→0.50), EPPE/oldwoj on 3.60/off 4.10; fastest-off vs slowest-on range 1.12→3.50, ratio 1.24×→1.97×. Top sprint 11.0 (arcadey, controllable), slow sprint 5.58 (not stuck). **No logic/stat/grabCap change**: per-char stat numbers untouched (all total 36), steal/dribble/defense constants untouched, grabCap still constant 48, kick power still charge+power. ⚠️ FLAG for feel-test: base speeds grew, so the flat `DEF_CLOSE_BONUS=0.06` is now a slightly smaller % of speed — left as-is per "don't touch defense"; bump it if walls feel they can't catch runners. Awaiting user feel-test before Phase 3.
- b55: **Phase 2 — 6-stat model implemented** (per BASED_CUP_STAT_DESIGN.md; Phase 3 menu NOT built). Replaced old 3-stat STATS with 6 assignable stats (on-ball, off-ball, power, defense, dribble, stamina) for all 14, each totalling 36 (exact doc numbers); added `paceOf()` (=on+off, display). `statDerived` now yields `maxOn`/`maxOff`/`kickPow`/`stamDrain`/`stamRegen`/`defense`/`dribble`. Movement split into on-ball vs off-ball base speed, wired into `applyMove` (human), `applyMoveAI` (CPU, keeps its 0.92 handicap), and `predictGuestSelf` (guest prediction). Dribbling scales `glueBall` tightness (GRAB 0.30→0.55). **Steal is now a CONTEST** (replaced the auto-strip): when the defender's identical grabCap overlaps a dribbling carrier, `p=clamp(STEAL_BASE+ (defense−dribble)*STEAL_WEIGHT, 0.05,0.85)` resolves it, gated by `STEAL_TRY_GAP`/`STEAL_COOLDOWN` so it's not spammable — even match 0.30 (genuine contest, no auto-strip), big defender edge ~0.66–0.72, big dribbler edge clamps to 0.05. Small `DEF_CLOSE_BONUS` (0.06/def pt) to off-ball speed when chasing the carrier. **Sacred re-proven:** `grabCap` still constant 48; grep shows NO stat in any radius/cap; kick power still `kickPow*(0.55+0.45*charge)`, no velocity. Tunables listed under Current build. Behaviorally verified in-browser (totals=36, pace, on/off split human+CPU, glue + steal scaling). **STOP for user feel-test before Phase 3.** Sprint/stamina interaction resolved (see Current build note).
- b54: **AI difficulty overhaul — tiers now genuinely escalate.** Audit: `aiControl` easy/normal/hard shared 100% of logic and differed only in `spd`/`err`/`kick`; `setAim` aimed every tier at the goal-line at ball.y (≈center, no difficulty term) → normal beelined off kickoff and shot dead-straight, feeling like easy. Fix (AI-logic only, no stats/grabCap): per-tier `D` table now also carries `react` (move-target re-decide cadence: easy 13f / normal 5f / hard 1f → felt reaction gap), `aimErr` (shot scatter .40/.16/.05), `corner` (how much to aim at the goal corner AWAY from the human keeper .15/.62/.95), `lead` (ball anticipation) and `defend` (goal-side interception .15/.55/1.0). Added a per-possession random "plan" (`aiCorner`/`aiLane`, refreshed on possession change + rarely mid-possession) so approach varies turn-to-turn (no fixed beeline). Shots now aim at a goal corner away from the keeper. **Behaviorally verified in-browser** from the worst-case center dribble: mean shot cross-Y easy 317≈center / normal 370 / hard 404≈post; spread SD 52/30/6; shot freq 186/299/386; 0 throws across 1200 shot sims + 2160 mixed calls. The beeline-and-straight-shot tell on normal is gone; all three clearly distinct, easy stays forgiving. Kept independent of the upcoming stat redesign.
- audit (b53, no change): **character-equality audit — both equalities PROVEN, no drift, no fix.** (1) Hitbox: `grabCap(pl){return PLAYER_R+BALL_R+11;}` ignores `pl` → constant 48 for all; `PLAYER_R=24` is a single const; every collision/possession site uses constants; `STATS` only feeds speed/power/stamina via `statDerived` (never a radius). Identical for all 14. (2) Rendered size: `drawPlayer` draws an identical frame (w=R·FACE_FIT/FACE_RF, h=ih·s) because every `_cut` PNG is exactly 360×450; measured each PNG directly — **chin line `bot=377` for all 14** (perfect chin alignment) and cheek-dominant faces hit `FACE_W=200` exactly (pepe/trumpe/doomer/sadjak/wojak ≈200). In-game head-bbox varies (pepe smallest ~57px H, obampe largest ~83px H) purely from hair/ears/beard extending past the normalized cheek line — **by design** (`process_sprites.py` normalizes cheek-width + chin only; "hair/ears extend naturally"). No fairness/size defect; touched nothing (per "don't rebuild what's correct"). Ship gate re-run clean (0 tokens, 0 placeholders, harness OK); no code change so no badge bump / no deploy.
- b53: **fixed the live `toLowerCase` of undefined @1094 TypeError (real CODE bug).** Mapped built `index.html`:1094 → template:1094 (1:1, both 3571 lines): the global `keyup` handler `keys[e.key.toLowerCase()]=false; keys[e.code.toLowerCase()]=false;` (and keydown @1091/1092). Root cause: real `KeyboardEvent`s always carry `key`/`code`, but **synthetic key events from browser autofill + extensions (Phantom wallet, password managers) arrive as generic events with `key`/`code` undefined** → `.toLowerCase()` threw, firing 2× (keydown+keyup) and at arbitrary times incl. mid-match. REPRODUCED in-browser (`dispatchEvent(new Event('keyup'))` → exact `@line:1094` throw), then confirmed GONE after the fix (synthetic events now fire 0 errors; real KeyboardEvents still work). Fix: guard `if(typeof e.key!=='string') return;` on both handlers + guard `e.code` independently (missing values are legitimately possible, so defensive guard is correct). Separately pixel-proved `drawPlayer` renders all chars in-match (pepe/chad/soyjak/wojak each draw 2.8k–4.1k head pixels) — the "blank in-match" was the earlier b46 cache, not a render bug. Harness passed while this shipped (it doesn't dispatch synthetic key events) — that's why prior sprite-file/cache audits missed it.
- b52: **"sprites disappeared" bug report — root-caused as NOT a build defect.** Diagnosed the 3 known causes in order: (1) all 14 `_cut`/`_cut_s.png` present + non-zero on disk (no OneDrive churn), (2) deployed `index.html` had 0 leftover tokens AND 0 `TINY` placeholders, all 14 `*_SRC` constants filled with real base64 PNGs, (3) `THRESH3=110` (correct). Runtime check in a real browser: all 14 `IMG` objects load at 360×450, `blankOrFailed:[]`, title scene renders a sprite. Conclusion: the b51 artifact renders all sprites — the live symptom is environmental (stale cache / b51 deploy not propagated; live was b46 before the recent push). Action: bumped badge → b52, clean rebuild + full ship gate, pushed to force a fresh Vercel deploy so the live build is confirmable. ⚠️ If the live badge still shows b46/blank after hard-refresh, the issue is the Vercel project's production-branch wiring, not the code. No code/sprite change was needed.
- b51 (deploy): **git wiring + first git-push deploy.** Made the working folder a real git checkout on `main` tracking `origin/main`; merged the deployed repo assets (`api/`, `package.json`, `supabase-schema.sql`, `README.md`) with the full local source via an unrelated-histories merge (only overlap was `index.html` — kept the validated b51, origin's old b46 stays in history). Pushed → `origin/main` `141b444`; Vercel auto-deploys from `main`. Deploy is now `git push` (was: manual upload) — CLAUDE.md updated. NOTE: the ~25 MB binary push takes >2 min — push in the background, don't kill on a foreground timeout. Next: TURN server or 2v2 when the user picks.
- b51 (docs): **docs consolidation** — three live docs (CLAUDE = laws, STATE = state, PROJECT_BRIEF = deep reference); archived the stale b24 deep briefing to `archive/HANDOFF.b24-snapshot.md` (its still-current infra/gotchas migrated into PROJECT_BRIEF §12–13); CLAUDE trimmed to 1 page of laws/guards/commands; STATE got this pinned end-of-session rule + status board. No game code changed (badge bump only).
- b50: crash-proof guard on `EXTRA_CHARS` (un-built file can't dead-screen) + root-caused the "won't start" bug = user was deploying `game_template.html` not `index.html`.
- b49/b48: on-screen error reporter (+ copy button).
- b47: head-soccer drawPlayer (no ring, face-centred, team pool).
- b46: face-normalised sprites (chin-aligned, consistent size) + per-char ADJUST.
- b45: invisible-face fix (THRESH3 110), neck/crop rework, ball rubber-band fix, intro tap-to-skip.
- b44: intro reveal. b43: 13-char art refresh. b41/b42: mobile layout + overlay scroll.
- b40: 60/55Hz tick + lower interp floors. b39: 7 more chars + WebRTC P2P. b38: warmed server-ping. b37: mobile control/aim fixes.
